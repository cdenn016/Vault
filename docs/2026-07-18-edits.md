# Research Vault Edits, 2026-07-18

## VFE 4.0 white paper geometry revision

The VFE 4.0 white-paper revision replaces the causal-token-graph base with the established general geometry of agents as local statistical-bundle sections over a noumenal or contextual base manifold. The language-model construction is formulated as the zero-dimensional reduction in which all labeled token agents are stacked over one base point and the causal graph records internal probabilistic dependencies among them.

The revision distinguishes latent sample bundles from statistical bundles, defines the correlated population recognition law as the exact ELBO object, recovers persistent local agent beliefs as its pushforward marginals, distinguishes those marginals from full conditionals used by population updates, and treats mean-field as an optional block-diagonal restriction. Same-point frame comparisons and any optional discrete graph connection are separated from curvature of the smooth base. The exact core uses the frame coboundary \(\Omega_{tj}=U_tU_j^{-1}\); independent graph links \(\Lambda_{tj}\) belong only to a separately declared extension. The diagonal \(G\) action inherited from the principal bundle over the point is separated from the internal population-frame reparameterization group \(G^{T+1}\).

The normalized language model now conditions on deterministic \(\Gamma_{\mathrm{core}}\), uses one normalized causal joint and one normalized correlated recognition law, and retains categorical cross-entropy as the observation sector of the negative ELBO. Random frames, graph links, or base-connection variables require their own proper prior, reference measure, recognition entropy, and normalization audit. The coordinate-update chapter states guarantees only for the same complete ELBO and distinguishes those guarantees from finite gradient steps, truncated optimization, and a separately normalized posterior-consensus objective.

Natural Gaussian coordinates are \((h,-J/2)\), their Fisher-dual expectation coordinates are \((\mu,M)\) with \(M=\Sigma+\mu\mu^\top\), and \((\mu,\Sigma)\) remains a moment, sampling, and reporting view. Source marginalization is identified as a mixture operation, so a source-conditioned Gaussian component is never conflated with the complete source-marginal law.

The prediction protocol now requires a target-blind, prefix-measurable prior predictor. Recognition may see the target during training but cannot replace the prior predictor during held-out scoring. A finite leakage property-test set is paired with a static causal-mask and dataflow audit. Identity population frames are tested only as a change-of-variables oracle; they are not a predictive ablation. Empirical attribution to the equivariant internal-map restriction therefore requires a genuinely different, capacity-matched unconstrained-map control.

The graphical abstract, factor graph, bundle-and-coordinate diagram, and verification ladder were redrawn or revised in TikZ. The figures visibly place the agent population over one base point, type the state and model sample copies, distinguish fixed core data from random variables, and separate natural, expectation, and moment coordinates. The master document also sets a valid header height and short chapter marks so the revised running heads do not collide with page content.

## Exact VFE 4.0 mean-field derivation

The ELBO chapter now derives the complete product-family theory obtained by factorizing the state, model, and categorical source coordinates of the fixed VFE 4.0 joint. The negative ELBO is written as initial complexity, expected categorical observation loss, two categorical source KL divergences, and source-weighted expected conditional transition KL divergences. Closed-form source-row coordinate updates and their log-sum-exp envelope are derived at the unit scale fixed by the ordinary ELBO.

The derivation retains the complete Markov blanket in the continuous state and model coordinate equations, including each receiving transition, the local observation, and all child or recoil transitions. The Gaussian specialization gives the full expected edge costs, including sender-covariance and model-covariance trace terms, and then expresses the conjugate transition sectors directly in natural information coordinates. The manuscript states that its categorical softmax emission remains nonconjugate, so no exact finite Gaussian information increment is claimed for that observation sector.

The comparison with V3 now separates three claims. The generic VFE 4.0 mean-field edge is an expected KL to a fixed conditional kernel and is not the live divergence between two moving recognition marginals. A restricted fixed-sender-covariance Gaussian choice recovers the V3 edge algebra up to the row constant $d/2$. A fixed-template row model also reproduces the unit-temperature V3 source-row decomposition exactly while its templates remain fixed, but refreshing those templates makes it a plug-in surrogate rather than coordinate ascent on one fixed global ELBO. A mixed-variation argument records the open-family obstruction, while the separate configuration-Gibbs model remains one exact construction that can house a fully live peer-consensus energy.

## Geometry-revision review

Independent geometry, exact-ELBO, and language-model causality reviewers examined the earlier geometry revision. The geometry review passed after the manuscript distinguished holonomy from its gauge-invariant conjugacy class and repaired the DAG notation. The ELBO review passed after a categorical source divergence was retyped from an invalid continuous-versus-categorical KL to the corresponding conditional categorical KL. The language-model review passed after the experimental controls, source schedule, leakage protocol, deterministic-geometry boundary, and held-out stopping rule were corrected. All three reviewers returned a final pass on that geometry-revision source.

## Verification

The complete manuscript was built with LuaLaTeX through `latexmk` from the manuscript directory, using the resolved manuscript bibliography:

```powershell
$env:WINDIR='C:\WINDOWS'
latexmk -lualatex -interaction=nonstopmode -halt-on-error -file-line-error -outdir="C:\tmp\research-vfe4-mf-20260718\build-baseline" VFE4_gauge_causal_elbo_whitepaper.tex
```

The final build produced 84 pages and 713,913 bytes with SHA-256 `A301778087A953CE0D427298A7E9DCEA3DED419914F6AB5CD1DDA9722A6EDEF3`. The log scan found zero unresolved or multiply defined references, citation warnings, overfull or underfull boxes, rerun requests, or header-height warnings. The white-paper source contains 222 unique labels and no duplicates. Separate scans found zero banned spacing macros, banned prose patterns, British spellings, placeholder markers, or known exactness regressions. `git diff --check` passed. The only remaining build messages are the pre-existing LuaLaTeX notice that `inputenc` is ignored and the pre-existing duplicate destination notice associated with restarting page numbering at `page.1`.

The new mean-field pages, printed pages 35 through 41, were rendered and inspected at 160 dpi. The equations, prose, running heads, and page boundaries are legible and nonoverlapping after splitting the longest fixed-template equation. Independent variational, information-geometric, and manuscript-integration reviewers returned final passes. Exact symbolic checks recovered the Gaussian compatibility residual (1/2) in the scalar case and a nonzero mixed third derivative for the Bernoulli peer energy, confirming the stated fixed-covariance constant and the generic open-family obstruction. The Research-vault lint covered 988 files and reported zero broken wikilinks, gray graph nodes, empty files, case collisions, or identity collisions.

## MAgent exact-ELBO white-paper design and production plan

A standalone MAgent white-paper design was added for a fixed normalized multi-agent state model, correlated population recognition law, exact state ELBO, explicit agent-block and fully factorized mean-field controls, Gaussian determinant-form evidence gap, and a separately typed configuration-level nested ELBO. The approved ontology retains agents as local section-bearing objects in statistical bundles associated to a principal bundle over a contextual base. Exact probability claims are limited to one context or a finite observation design, so the design does not infer an infinite-dimensional section-space measure from pointwise formulas.

The production plan fixes the proof order, module boundaries, citation responsibilities, three TikZ figures, durable NumPy and SymPy verification oracles, executable-code crosswalk, independent specialist review, final verifier gate, and clean LaTeX, bibliography, PDF, and vault checks. It also preserves the existing VFE 4.0 paper, PIFB2 manuscript, MAgent executable and active configuration, wiki surfaces, and dirty live Research checkout. The plan stops before push, merge, live-checkout synchronization, or worktree removal because those Git lifecycle actions were not part of the writing request.

## MAgent exact-ELBO white-paper shell

The standalone MAgent master document and thirteen final chapter modules were created under `manuscripts/MAgent_exact_elbo_whitepaper.tex` and `manuscripts/magent_elbo_whitepaper/`. The shell uses the approved report metadata, stable module ordering, a dedicated bibliography call, and no VFE 4.0 module inputs. Each module states its proof role and preserves the boundary between agents as local statistical-bundle sections, finite-context latent samples, a fixed normalized state model, correlated recognition, restricted mean-field families, and the separately typed configuration tier.

The shell was smoke-built from `manuscripts` with LuaLaTeX through `latexmk` into `C:\tmp\magent-exact-elbo-whitepaper-build-20260718`. With `WINDIR` set to `C:\WINDOWS` for the TeX Live launcher, the command exited `0` and produced a 23-page PDF. The verified PDF was copied to `manuscripts/MAgent_exact_elbo_whitepaper.pdf` without staging the binary.

## MAgent exact-ELBO citation inventory verification

The external mathematical source inventory for the standalone MAgent exact-ELBO white paper was checked against publisher, DOI, journal, or author-hosted records. All eighteen specified citation keys already exist in `manuscripts/references.bib`, and their authors, titles, dates, and publishers or venues agree with the checked metadata. No required record was absent or demonstrably incorrect, so the bibliography was deliberately left unchanged. The inventory assigns variational-inference claims to Blei, Bishop, and Wainwright; entropy and divergence claims to Cover and Thomas, Kullback and Leibler, and Csiszár; information geometry to Amari and Amari--Nagaoka; bundle geometry to Nakahara and Kobayashi--Nomizu; finite-step manifold optimization to Absil, Mahony, and Sepulchre; SPD and matrix claims to Horn and Johnson and Bhatia; graphical-model semantics to Lauritzen and Wainwright; generalized Bayes to Bissiri, Holmes, and Walker; Bethe approximations to Yedidia, Freeman, and Weiss; and free-energy terminology to Friston.

An isolated temporary `\\nocite` closure was compiled with BibTeX against the resolved bibliography. It completed with all eighteen keys and no missing-entry or BibTeX syntax error. A whole-file key scan also found no exact or case-folded duplicate keys. The temporary source was removed from the designated build directory after the check; neither it nor the untracked delivered PDF was staged.

## MAgent bundle geometry and finite-context probability types

The MAgent white paper now states its new exact finite-context theory without claiming that the active executable already realizes that theory. Its claim taxonomy distinguishes definitions, identities, propositions, legacy theorems, implementation facts, verification targets, and open proposals. The geometry uses one principal bundle with state and model actions represented from the same frame change; it defines associated statistical bundles through pushforwards of probability measures, then specializes to nondegenerate Gaussian fibers for analytic calculations. Agents remain local section-bearing objects, while finite samples and graph vertices retain their separate types. Regime-I frame comparisons are explicitly flat coboundaries, whereas only independently declared Regime-II interaction links can carry nontrivial discrete holonomy.

The probability chapter evaluates sections only on a declared finite design, declares the observation and latent product spaces, reference measures, normalized generative and correlated recognition kernels, support conditions, and the absence of any implied continuum section-space probability measure. The appendix notation table distinguishes all geometry, state, and configuration objects. Its configuration recognition law is typed as the observation-indexed kernel \(R:\mathsf O_D\to\mathcal P(\mathsf X)\), rather than incorrectly as one unconditional probability measure.

The manuscript was built from `manuscripts` with LuaLaTeX through `latexmk` into `C:\\tmp\\magent-exact-elbo-whitepaper-build-20260718` using `-interaction=nonstopmode -halt-on-error -file-line-error`. The build exited `0` and produced a 32-page, 279,689-byte PDF. The log contained zero undefined citations, undefined references, multiply defined labels, rerun requests, or missing-character warnings. The Task 3 source scan found zero banned spacing macros, banned stock phrases, British spellings, or unfinished-work markers; 40 owned labels were unique, and the required bundle, transport, finite-product, recognition, and configuration-kernel type links were present. `git diff --check` passed. The log retains two overfull chapter-heading warnings in existing material, one in Chapter 4 and one in Appendix B; neither arises from a Task 3 changed line.

## MAgent probability-kernel type corrections

The Task 3 gate correction fixes the configuration tier on one measurable space \((\mathsf X,\mathscr X)\) whose design, incidence pattern, and identified latent and observation fibers do not vary with \(X\). The generative family is now an honest kernel on the resulting fixed measurable spaces, with kernel notation \(P_\theta(do,dY\mid X)\) separated from the dominated density identity \(P_\theta(do,dY\mid X)=p_\theta(o,y\mid X)\nu_D^O(do)\nu_D^Y(dy)\). The configuration prior and recognition law are typed as \(P_0\in\mathcal P(\mathsf X,\mathscr X)\) and \(R:(\mathsf O_D,\mathscr O_D)\to\mathcal P(\mathsf X,\mathscr X)\), with the defining kernel measurability condition stated explicitly.

The geometry correction restricts \(Y(c)\) to the common agent support, states the Regime-I cocycle identity only on triple overlaps, and assumes closure of each statistical family under the represented pushforward; the Gaussian specialization satisfies this closure under invertible linear actions. The three flagged displays now carry standard equation punctuation. A complete scan of the four Task 3 modules finds no remaining mixed \(p_\theta(o,dY\mid X)\) notation.

The corrected manuscript was rebuilt with LuaLaTeX through `latexmk` into `C:\\tmp\\magent-exact-elbo-whitepaper-build-20260718`. The final build exited `0` and produced a 33-page, 283,802-byte PDF with SHA-256 `7E65B69B19C9E2B7F597829DECDFF8ADFEFC0C3751A1C028DEDEE90077630080`. The log scan found zero undefined citations, undefined references, multiply defined labels, rerun requests, missing-character warnings, or underfull boxes. The two existing overfull chapter-heading warnings remain in Chapter 4 and Appendix B. LuaLaTeX also repeats the existing notice that `inputenc` is ignored and the existing duplicate-destination notice for `page.1`. The Task 3 scan found 41 unique labels with no duplicates, 12 citation keys with no missing bibliography records, zero banned spacing macros, banned stock phrases, unfinished markers, British spellings, or horizontal rules, and no type-flow failures. `git diff --check` passed.

## MAgent bundle smoothness and pointwise loop correction

The general associated statistical-bundle construction now distinguishes the measurability needed to define probability pushforwards from the smoothness needed to form smooth associated bundles. The represented sample-fiber actions and their induced pushforward actions are required to be smooth in the selected sample and statistical-manifold coordinates, while each statistical family must remain closed under those actions. The nondegenerate Gaussian specialization explicitly satisfies closure and smoothness in \((\mu,\Sigma)\) coordinates under smooth invertible linear representations.

The Regime-I comparison, cocycle, represented channel maps, and gauge law now carry their context argument explicitly. The closed-walk identity is stated only at one fixed context in the common intersection of every participating agent support, and every factor in the telescoping display is evaluated at that context. The text also states that comparisons made at different contexts require base parallel transport to a common fiber and do not telescope by the same argument.

The complete manuscript was rebuilt with LuaLaTeX through `latexmk` into `C:\\tmp\\magent-exact-elbo-whitepaper-build-20260718`. The final build exited `0` and produced a 33-page, 285,234-byte PDF with SHA-256 `8682884E84E259BE291EC79687B35257DFFE2882464FDE90643E197CEB5D5858`. The log contains zero undefined citations, undefined references, multiply defined labels, rerun requests, missing-character warnings, or underfull boxes. The two existing overfull chapter-heading warnings remain in Chapter 4 and Appendix B. LuaLaTeX also repeats the existing notice that `inputenc` is ignored and the existing duplicate-destination notice for `page.1`.

The four-module Task 3 scan found zero banned spacing macros, banned stock phrases, unfinished markers, British spellings, horizontal rules, or mixed density/measure notation. It found 42 labels, all unique, and 12 citation keys, all present in `manuscripts/references.bib`. The type-flow scan passed the smooth represented actions, family closure, Gaussian closure and smoothness, common-support population, triple-overlap cocycle, single-context closed walk, different-context parallel-transport boundary, finite product, fixed configuration space, generative-kernel measurability, dominated density, correlated recognition, configuration recognition, and Regime-II gauge-law checks. `git diff --check` exited `0`; Git emitted only its platform line-ending conversion notices.

## MAgent fixed normalized two-channel generative model

The generative-model chapter now defines the authoritative object as the normalized probability kernel \(P_\theta(do,dY\mid X)\). When domination holds, the chapter uses only the fully dominated density \(p_\theta(o,y\mid X)\) against \(\nu_D^O(do)\nu_D^Y(dy)\); it does not mix density and measure notation. The abstract finite-DAG construction declares normalized model, state, and observation kernels with explicit root conventions, while the finite-design reference law declares conditional independence across design points through a product of the one-point kernels. Every generative factor is fixed by \((\theta,X)\), and recognition laws, posterior marginals, and live peer beliefs are excluded from its inputs.

The analytic reference specializes the directed structure to a forest and gives proper nondegenerate Gaussian model roots, root state bridges, nonroot model transitions, nonroot state transitions, and observation kernels. Every domain, matrix shape, offset, and SPD covariance is stated. A reverse-topological proof integrates observations first, then each nonroot state and model pair after its descendants, and finally the root state bridges and root model laws. The loopy alternative is stated only as a Gibbs density with the existence condition \(0<Z_X<\infty\).

One principal-frame change \(g_i\) induces the two represented actions \(g_i^k=\rho_k(g_i)\) and \(g_i^m=\rho_m(g_i)\). The manuscript derives the link, self-map, bridge, offset, root-moment, covariance, precision, observation-map, and observation-covariance laws from these represented actions. Covariances transform by congruence, precisions by dual inverse congruence, and the gauge-inert observation covariance remains unchanged. The appendix checks the transformed residual quadratic forms, determinant Jacobians, root moments, and observation means.

The complete manuscript was built with LuaLaTeX through latexmk into C:\tmp\magent-exact-elbo-whitepaper-build-20260718. The build exited 0 and produced a 38-page, 321,361-byte PDF with SHA-256 9761D1EDA987ECED2207A8F78EE0D003F7579FBE345F73328DAE27BE607A4C68; the verified PDF was copied to the untracked delivery path. The log has zero hard errors, undefined citations, undefined references, multiply defined labels, rerun requests, amsmath warnings, missing characters, or underfull boxes. It retains the two previously recorded overfull chapter-heading warnings.

The Task 4 source scan found zero mixed density/measure notation, banned spacing macros, banned stock phrases, horizontal rules, British spellings, or missing citation records. The recognition symbols \(Q_X\), \(q_i\), and \(s_i\) occur only in the explicit posterior-independence prohibition. All 80 labels across the chapter modules are unique, all source references resolve to module labels, and the four Task 4 citation keys are present in manuscripts/references.bib. A three-node, nine-variable two-channel Gaussian forest gave joint-precision minimum eigenvalue \(0.279427940806\) and integrated mass \(0.9999999999999969\), an absolute normalization error of \(3.109\times10^{-15}\). Independent numerical gauge checks placed every reported covariance, precision, root-moment, and observation residual below \(3.6\times10^{-14}\). The Git whitespace check passed.

## MAgent correlated recognition and exact state ELBO

The structured-recognition chapter now types \(Q\) as a normalized Markov kernel measurable jointly in the observation and structural value. The local state and model recognition sections are defined only as coordinate pushforwards. The text states that those marginals leave the general dependence law undetermined, including the continuous-coordinate copula and Gaussian off-diagonal covariance blocks. In the finite Gaussian reference regime, the chapter gives the exact information-form log normalizer, derives \(\mu=J^{-1}h\) and \(C=J^{-1}\), and distinguishes precision off-diagonal blocks as conditional-dependence parameters from covariance blocks.

The state evidence theorem now states kernel and density measurability, normalization, positive finite evidence, recognition-to-posterior absolute continuity, log-integrability, and the stronger factorwise integrability needed to split expectations. The appendix derives the ELBO/KL identity by the Radon--Nikodym chain rule and proves that equality requires coincidence of the complete recognition and posterior measures. The negative ELBO is expanded once into root model laws, root state bridges, nonroot model transitions, nonroot state transitions, observation factors, and one reference-relative joint recognition term. The appendix counts \(2M|\mathcal R|+2M(N-|\mathcal R|)+MN=3MN\) fixed generative factors and shows that the monolithic and factorized free energies have zero algebraic residual without replacing the joint entropy by marginal entropies.

The optimization section separates posterior-conditional E-coordinates, exact M-coordinate maximizers, accepted generalized-EM changes, and finite optimization proposals. Evidence monotonicity is assigned only to the classical schedule that pairs the old-parameter exact posterior with an accepted increase of the same ELBO. Natural-gradient and ordinary optimizer directions are stated to have no finite-step monotonicity guarantee without an objective-based acceptance rule.

The symbolic factor residual simplified exactly to zero and the symbolic count simplified to \(3MN\). A finite binary two-agent, two-channel model gave log evidence \(-1.8212320501867609\), ELBO \(-2.6864298581692858\), and posterior KL \(0.8651978079825249\). The factorized-versus-monolithic free-energy residual was \(4.441\times10^{-16}\), and the ELBO-plus-KL evidence residual was zero at printed precision. For \(J=\left[\begin{smallmatrix}2.5&-0.7\\-0.7&1.8\end{smallmatrix}\right]\) and \(h=(0.4,-0.6)^\top\), independent two-dimensional quadrature returned normalized mass \(1.0000000000000000\) with estimated quadrature error \(9.515\times10^{-11}\), agreeing with the analytic log normalizer.

The complete manuscript was built with LuaLaTeX through latexmk into C:\tmp\magent-exact-elbo-whitepaper-build-20260718. The post-verifier build exited \(0\) and produced a 42-page, 403,883-byte PDF with SHA-256 6FD553C7C30F85CA56915C79DDB37D01ED5298F410C82F457053428B358486D5; the verified PDF was copied byte-identically to the untracked delivery path. The log has zero hard errors, undefined citations, undefined references, duplicate labels, rerun requests, amsmath warnings, missing characters, or underfull boxes. It retains the same two previously recorded overfull chapter-heading warnings, one in Chapter 4 and one in Appendix B. The source scan found zero banned spacing macros, banned prose patterns, British spellings, placeholders, horizontal rules, mixed density/measure notation, duplicate labels, unresolved source labels, or missing bibliography keys. An independent verifier passed the kernel typing, Radon--Nikodym proof, Gaussian normalizer and moments, \(3MN\) count, single joint-entropy term, and E/M/generalized-EM guarantee scope with no blocking mathematical finding. The Git whitespace check passed.

## MAgent explicit mean-field theory

The MAgent mean-field chapter now defines agent-block and state--model product recognition families over each agent's complete finite-design coordinates. These are explicit restrictions of the correlated state recognition law and do not alter the fixed normalized generative model or the ontology of agents as local section-bearing objects. A constrained functional variation derives the normalized CAVI coordinate with its negative-log-integral constant, finite normalizer, measurability, integrability, and existence assumptions. The state, model, and combined agent coordinates retain their complete directed Markov blankets, including every outgoing child factor. Receiver-only updates that omit those child sums are identified as local surrogates rather than exact CAVI.

For a Gaussian posterior \(\mathcal N(m,J^{-1})\) and any declared block partition, the chapter derives the reverse KL from the two normalized densities and proves the unique optimum \(\mu^\star=m\) and \(C_b^\star=J_{bb}^{-1}\). The covariance is determined by diagonal posterior-precision blocks rather than posterior marginal covariance. Substitution yields the evidence gap \(\frac12\log[(\prod_b\det J_{bb})/\det J]\); a Schur-complement proof of the block Fischer inequality establishes nonnegativity and equality exactly for block-diagonal \(J\). The agent-block and state--model partitions are treated separately.

The Gaussian precision audit expands every model transition, state transition, root bridge, and local observation Hessian. The agent-block control removes or exactly cancels the complete cross-agent posterior-precision blocks while retaining within-agent bridge and observation curvature. The finer control also removes or cancels the total within-agent state--model block, including \(-\Lambda_i^kB_i\), \(H_i^\top(R_i^o)^{-1}L_i\), and any additional local coupling. Each control proves membership of the exact posterior in its own restricted family and a zero determinant gap. The manuscript states that one zero edge coefficient or a small numerical coupling proves neither result.

The fixed-source auxiliary row is now a normalized joint law \(P_i(y,j)=\pi_{ij}f_{ij}(y)\) compared with \(Q_i(y,j)=\beta_{ij}q_i(y)\). Its joint KL decomposes exactly into the source-weighted state KLs plus \(\KL(\beta_i\Vert\pi_i)\), and simplex variation yields the normalized unit-temperature softmax row. The component densities must remain fixed and normalized. Nonunit temperature requires a separately normalized tempered model, refreshing components from live recognition marginals changes the model, and the categorical, state, and configuration entropy sectors remain distinct.

Exact SymPy checks returned zero residual for the state-transition Hessian, the complete state and model child-blanket derivatives, the row-KL decomposition, row normalization, and row-softmax stationarity. A rational four-dimensional SPD example had block determinant ratio \(19/16\), exact optimal trace \(4\), and a nonzero difference between \(J_{bb}^{-1}\) and \((J^{-1})_{bb}\). The agent-block control had determinant ratio one while its finer state--model ratio remained \(441/400\); after exact local cross-block cancellation, the finer ratio was one. A six-dimensional numerical SPD oracle had minimum eigenvalue \(0.964148671651171\), determinant gap \(0.629570738291683\), direct-versus-formula residual \(8.882\times10^{-16}\), and maximum optimal block-gradient norm \(1.278\times10^{-15}\).

The final manuscript was built with LuaLaTeX through latexmk into `C:\tmp\magent-exact-elbo-whitepaper-build-20260718\task6`. The build exited zero and produced a 51-page, 456,559-byte PDF with SHA-256 `A6F9B9D41AABDF728B6831A70EFADD23B7E023A440B7E7EB5AC99BB17D554EA2`; the untracked delivery copy is byte-identical. The log contains zero hard errors, undefined citations, undefined references, duplicate labels, rerun requests, amsmath warnings, missing characters, or underfull boxes. It retains only the two previously recorded overfull chapter-heading warnings in Chapter 4 and Appendix B. The complete module set contains 164 unique labels with no duplicates, 64 unique reference targets with none unresolved, and 15 cited bibliography keys with none missing. Separate scans found zero banned spacing macros, banned prose patterns, horizontal rules, British spellings, or placeholders. `git diff --check` passed. The independent variational investigator's corrections were incorporated, and the separate verifier returned a full pass with no Critical, Important, or Minor finding.

## MAgent exact hierarchical configuration ELBO

The configuration chapter now places the finite-context state model inside a second normalized probability layer. A proper design prior $P_0$, the fixed conditional state kernel $P_\theta(do,dY\mid X)$, and the observation-indexed configuration recognition kernel $R(dX\mid o)$ define one joint recognition measure $R(dX\mid o)Q_X(dY\mid o)$. The chapter states the measurable-space, absolute-continuity, positive-evidence, and log-integrability assumptions before deriving the exact joint Kullback--Leibler identity. The resulting nested ELBO contains one state-level entropy sector and one configuration-level entropy sector, and equality holds exactly when the complete joint recognition measure equals the joint posterior. A source entropy already represented inside $Y$, or inside $X$ when the source variable is promoted to the configuration, is therefore not added again.

The chapter separates the ordinary hierarchical ELBO from an optional observation-free Gibbs configuration prior. The common positive scale $T$ leaves the optimizer of a fixed ordinary ELBO unchanged, while a Gibbs prior proportional to $\exp[-\mathcal F_{\mathrm{vac}}(X)/T]$ changes the normalized model with $T$. The Gibbs construction requires a proper base measure, a measurable finite energy almost everywhere, and a finite nonzero partition function. The noncompact $\mathrm{GL}^{+}(K)$ boundary is explicit: an unrestricted Haar factor is not a probability prior, so an exact construction must use a proper prior or a declared quotient or gauge-fixing prescription with the required Jacobian or Faddeev--Popov factor and treatment of stabilizers and singular strata. The appendix proves both the hierarchical joint-KL identity and the separate Gibbs variational identity directly from their normalized measures.

An exact finite discrete oracle used $P_0=(2/5,3/5)$ and a rational two-configuration, three-state observation model. It returned evidence $11/25$, posterior configuration weights $(7/22,15/22)$, zero nested-identity residual, zero entropy-sector residual, zero posterior joint KL, and zero posterior equality residual. The finite candidate argmin was the exact posterior at both $T=2$ and $T=7$, confirming optimizer invariance for the same fixed ordinary ELBO. A read-only configuration-chain investigator and a separate verifier passed the measure and density types, assumptions, signs, equality condition, entropy count, temperature boundary, Gibbs normalization, noncompact-group boundary, appendix proofs, citations, punctuation, and style with no mathematical finding.

The complete manuscript was built with LuaLaTeX through `latexmk` into `C:\tmp\magent-exact-elbo-whitepaper-build-20260718\task7`. The build exited zero and produced a 58-page, 500,902-byte PDF with SHA-256 `36CE214C3145327FA5C89E77A78800C7C3A572CC9DEFABCC067BE0DA4762B833`; the untracked delivery copy is byte-identical. The log contains zero hard errors, undefined citations, undefined references, duplicate labels, rerun requests, missing characters, underfull boxes, or overfull vertical boxes. It retains only the two previously recorded overfull chapter-heading warnings in Chapter 4 and Appendix B. The complete module set contains 200 unique labels with no duplicates, 85 unique reference targets with none unresolved, and 19 cited bibliography keys with none missing. Separate scans found zero banned spacing macros, banned prose patterns, horizontal rules, British spellings, or placeholders. `git diff --check` passed. Printed pages 28 through 32 and 39 through 43 were rendered at 130 dpi and inspected; the configuration derivation and both appendix proofs are legible, unclipped, and nonoverlapping.
