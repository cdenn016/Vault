<!-- rigorous-theory-search-metadata {"contract_id":"contract-sha256-b6f7aeefd57a2cac70495af4744dd3e3f58dccfe630d6407e3e277f34dac526b","schema_version":"rigorous-theory-search/v1","target_digest":"b6f7aeefd57a2cac70495af4744dd3e3f58dccfe630d6407e3e277f34dac526b"} -->
# Counterexample register

This checkpoint records quantifier-matched falsifiers. `CANDIDATE` rows remain
uncertified until an independent derivation reproduces their hypotheses and
calculation; `EVIDENCE_VERIFIED` rows have that artifact-backed reconstruction.
Failure to find another witness is not supporting evidence.

| ID | Claims attacked | Typed witness and decisive equation | Assumption boundary | Status |
| --- | --- | --- | --- | --- |
| CE-ACTION-LP | `action-analyticity` if enlarged to an arbitrary finite-$L^p$ neighborhood; `score-action-compatibility` if its domain is identified with the exponential-action chart | Under $\gamma=N(0,1)$, the odd Hermite direction $\varphi=\mathrm{He}_3(x)=x^3-3x$ lies in $L^2_0(\gamma)$ with $\E[\mathrm{He}_3]=0$ and $\E[\mathrm{He}_3^2]=6$, yet $N_3(t)=\int e^{-t\mathrm{He}_3}d\gamma=+\infty$ for **every** $t\neq0$, because $-t\mathrm{He}_3(x)-x^2/2\sim t\lvert x\rvert^3\to+\infty$ as $x\to-\infty$ for $t>0$ and as $x\to+\infty$ for $t<0$. The same holds for every odd $\mathrm{He}_k$, $k\geq3$. The degree-one statement $\varphi=-x$ and the $t=1$ statement that $e^{x^2}$ is nonintegrable remain true and are the weaker facts. | The nonlinear chart must remain an open bounded $L^\infty$ chart unless a stronger uniform-moment theorem is proved. **Correction (2026-08-05):** $\varphi=-x^2$ is *not* a valid witness for absence of a two-sided exponential-action neighborhood, since $\pi(e^{tx^2})=(1-2t)^{-1/2}$ is finite on the two-sided interval $(-\tfrac12,\tfrac12)$, taking the values $\sqrt6/3$ at $t=-\tfrac14$ and $\sqrt2$ at $t=\tfrac14$. Any restatement using the degree-two direction must be replaced by the odd-$\mathrm{He}_k$ witness above. | EVIDENCE_VERIFIED |
| CE-ACTION-NORMALIZATION | `action-derivative`; identification of mass-tracking and normalized full-space actions | Normalizing $e^{-\varphi}\pi$ adds $\log\pi(e^{-\varphi})$ to the coarse action, so $D\widehat Q(0)h=Uh-\pi h$ and $D^2\widehat Q(0)=-\operatorname{Cov}(\cdot\mid Z)+\operatorname{Cov}_\pi$. | The mass-tracking and probability-normalized actions agree only modulo constants. | EVIDENCE_VERIFIED |
| CE-CIRCLE-NORM | `circle-norm-witness`; every topology-free relevance statement | For $Uf=f\circ D$ with $D(x)=2x\bmod1$, Haar preservation gives $r_{L^\infty}(U)=1$, while $[f\circ D^n]_\alpha\leq2^{\alpha n}[f]_\alpha$ and a fixed Fourier mode give $r_{C^\alpha}(U)=2^\alpha$. | The operator formula and measure are identical; only the declared topology changes. | EVIDENCE_VERIFIED |
| CE-HOLDER-NONLATTICE | `essential-spectrum` if applied to the circle Holder space | Although $|\sin(2\pi Nx)|\leq1$, its standard Holder norm diverges with $N$ while the norm of $1$ is fixed, so the norm is not a lattice norm. | The circle calculation is a separate Banach-space witness, not an instance of the qualified Banach-lattice theorem. | EVIDENCE_VERIFIED |
| CE-DOBRUSHIN-NONNECESSARY | `dobrushin-criterion` if called necessary | The compatible reverse matrices $R_0=((1,0),(0,1),(1/2,1/2))$ and $R_1=((1/2,1/2,0),(0,0,1))$ each have coefficient one, but $R_1R_0$ has two identical rows and quotient norm zero. | Accumulated Dobrushin coefficients give an upper certificate, not the actual cocycle norm. | EVIDENCE_VERIFIED |
| CE-DOBRUSHIN-ONE-STEP | `dobrushin-criterion` if one-step strictness is called sufficient | For $R_k=(1-a_k)I+a_kJ$ with $a_k=2^{-k-2}$ and uniform reset $J$, every $\delta_k=1-a_k<1$ but $\prod_k\delta_k>0$. | Nonautonomous forgetting requires an accumulated negative rate relative to $B_{n\leftarrow\ell}\to\infty$. | EVIDENCE_VERIFIED |
| CE-PRODUCT-DIAGONAL | `interaction-gauge-domination`; automatic availability of `hoeffding-inverse` after arbitrary law-level coarse-graining | Clone a uniform bit by $x\mapsto(x,x)$. Any product law equivalent to the two-positive-atom diagonal output would make both marginals charge both bits and would therefore give positive off-diagonal mass. The nonatomic diagonal embedding gives the analogous singular witness. | Exact law pushforward survives; product-reference equivalence is a separate per-scale admission premise and can fail for every product target. | EVIDENCE_VERIFIED |
| CE-HOEFFDING-SHEAR | `interaction-gauge-domination`; preservation of interaction degree by any product-measure-preserving gauge action | The Haar-preserving torus shear $(x_1,x_2)\mapsto(x_1,x_1+x_2)$ sends a nonconstant singleton function of $x_2$ to a function of both coordinates. | Product-measure preservation alone does not intertwine the Hoeffding grading. The theorem uses componentwise/permutation realization as a sufficient grading-intertwining hypothesis without claiming uniqueness. | EVIDENCE_VERIFIED |
| CE-NONPRODUCT-AVERAGING | `hoeffding-inverse` if equivalence is used without a product averaging law | Under the two-point diagonal law on $\{0,1\}^2$, the off-diagonal indicator is the zero $L^\infty$ class, but coordinate averaging its displayed representative against a marginal can be nonzero. | Mutual absolute continuity identifies classes and norms only after a product law is supplied; it does not itself define representative-independent coordinate averaging. | EVIDENCE_VERIFIED |
| CE-WEIGHTED-FRECHET | weighted-one-space unification of `essential-spectrum` with `action-analyticity` | On $c_0(5^n)$, the advance-or-reset reverse operator has $U\varphi=2\varphi$ for $\varphi_n=1/3+(2/3)4^n$, yet weighted-norm spikes make $\|Q(f)-Uf\|_w/\|f\|_w$ fail to tend to zero. | A relevant bounded linear operator need not be the Frechet derivative of the nonlinear logarithmic action map on that weighted chart. | CANDIDATE |
| CE-HORIZONTAL-ANOMALY | `pullback-compatibility` without `horizontal-defect-anomaly` | On a trivial line bundle over $\mathbb R$ with the unit-variance normal location fiber, use the identity bundle map (so $\Delta_F^\Psi=0$), exactly related constant sections, source horizontal field $\partial_x$, and target field $\partial_x+a\partial_\mu$ with $a\neq0$. Then $u=0$, the horizontal defect is $-a\partial_\mu$, and $h_s^\omega-f^*\bar h_{\bar s}^{\bar\omega}=-a^2dx^2\prec0$: strict negativity with a genuine Markov fiber map, exactly related sections, and both Fisher metrics positive definite. | Zero horizontal defect is **sufficient** for base positivity and is **not necessary**. The exact criterion is $2\bar g^F(T^V\Psi D^\omega sX,A_\Psi(s;X))+\lVert A_\Psi(s;X)\rVert^2_{\bar g^F}\leq\delta_\Psi(X,X)$ for every $X$. On the fine fiber $\{\mathcal N(\mu,1)\}$ with kernel $N(x,\cdot)=\mathcal N(x,1)$, $f=\mathrm{id}$, section $s(x)=\mathcal N(x,1)$, and anomaly $b\partial_\mu$, the comparison is $1-\tfrac12(1+b)^2$, which is $\tfrac{79}{200}>0$ at $b=\tfrac1{10}$ and $\tfrac{23}{25}>0$ at $b=-\tfrac35$ with nonzero anomaly, and $-\tfrac18<0$ at $b=\tfrac12$. The triangle-inequality margin is likewise sufficient and not necessary: at $b=-\tfrac35$ it is violated ($\tfrac{3}{5\sqrt2}>1-\tfrac1{\sqrt2}$) while the comparison is positive. | EVIDENCE_VERIFIED |
| CE-SECTION-DESCENT | `configuration-projectability` | Collapse $S^1$ to one point, a surjective submersion, use the identity fiber map on the unit-variance normal location fiber, and take $Q(x)=\mathcal N(\sin x,1)$. Here $A_\Psi=0$ and $\ker Tf=TS^1$, while $T^V\Psi(D^\omega Q\partial_x)=\cos x\,\partial_\mu\not\equiv0$, so the projectability criterion fails and no section over the point satisfies $\Psi Q=\bar Q f$. In the $L^2(S^1)$ tier the descendable set is exactly the constants: closed, of infinite codimension, with empty interior. | A bundle morphism does not induce a configuration map on all section spaces. **Scope:** the empty-interior statement is for a *collapsing* base map; if $f$ is a diffeomorphism every section descends, so no unscoped claim that it induces a map nowhere is available. | EVIDENCE_VERIFIED |
| CE-NONSMOOTH-DESCENT | `configuration-projectability` if the submersion hypothesis is dropped | $\mathcal C=\bar{\mathcal C}=\mathbb R$, $f(x)=x^3$ a smooth bijection that is not a submersion at $0$, trivial bundle with fiber $\{\mathcal N(\mu,1)\}$, $\Psi$ the identity on fibers, $Q(x)=\mathcal N(x,1)$. The unique descent $\bar Q(y)=\mathcal N(y^{1/3},1)$ is continuous but not differentiable at $y=0$. | Smoothness of the descended section is a theorem only under a surjective smooth submersion; the submersion hypothesis is load bearing, not decorative. | EVIDENCE_VERIFIED |
| CE-BASE-COCYCLE-RESIDUAL | `bundle-scale-cocycle`/`base-defect-cocycle` if the base Fisher-defect cocycle is asserted additively, and any convention-mixed correction term | Three normal location fibers of variances $1,2,3$ (so $g_0^F=1$, $g_1^F=\tfrac12$, $g_2^F=\tfrac13$), sections $x\mapsto x$, $y\mapsto y/2$, $z\mapsto z/6$, base maps $f_{01}(x)=2x$ and $f_{12}(y)=3y$, identity fiber maps, and local connection forms $0$, $a_1dy$, $a_2dz$. The exact residual is $\delta_{02}-\delta_{01}-f_{01}^*\delta_{12}=-\tfrac23a_1(a_1+1)$, nonzero for $a_1\notin\{0,-1\}$; at $a_1=\tfrac1{10}$ it is $-\tfrac{11}{150}$. A correction written with the *pushed fine* jet in the cross terms and a $+$ sign on the quadratic term evaluates to $+\tfrac23a_1(a_1-1)=-\tfrac3{50}$ there, exceeding the true residual by exactly $\tfrac43a_1^2=2\Delta_F^{\Psi_{12}}(A,A)$. | Only two conventions are correct: coarse jet in the cross slots with $+\Delta(A,A)$, or pushed fine jet with $-\Delta(A,A)$. Mixing them inflates the residual by two copies of the second-arrow vertical defect on the first-arrow anomaly. The sharp cocycle holds exactly under equality of the two $\Delta_F^{\Psi_{12}}$-seminorms; at $a_1=-1$ it holds with anomaly $-2\neq0$, so vanishing stage-one anomaly is sufficient and not necessary. A telescoping identity in $h_j-f^*_{jk}h_k$ is unconditional and tests nothing here. | EVIDENCE_VERIFIED |
| CE-TRIVIALIZATION | `generalized-modes` without full tempered comparison control | Let every derivative be the identity on $\mathbb R$, let $b_k=e$, and take $J_ku=e^{k^2}u$. Then $\widehat M_k=e^{-2k-1}$, $\widehat M_{n\leftarrow0}=e^{-n^2}$, and $s_{n\leftarrow0}=n$, so the apparent reference exponent is $-n\to-\infty$ while the native exponent is zero and the scalar normalization is tempered. | Tempering only scalar mode representatives is insufficient; the full comparison trivialization and its inverse must satisfy the two-sided subexponential bound relative to cumulative log block scale. | EVIDENCE_VERIFIED |
| CE-MODE-ORDER | `cocycle-law` if the derivative product is unordered or reversed | $A=\begin{psmallmatrix}1&1\\0&1\end{psmallmatrix}$ and $B=\begin{psmallmatrix}1&0\\1&1\end{psmallmatrix}$ give $BA=\begin{psmallmatrix}1&1\\1&2\end{psmallmatrix}\ne AB=\begin{psmallmatrix}2&1\\1&1\end{psmallmatrix}$. | The rightmost factor acts first; a two-parameter cocycle is not a semigroup and a periodic scheme has monodromy rather than a one-step map. | EVIDENCE_VERIFIED |
| CE-CROSS-SPACE-EIGEN | `generalized-modes` if written as an ordinary eigenvalue equation | With $X_0=\mathbb R$, $X_1=\mathbb R^2$, and $M_0x=(x,0)$, the expression $M_0v=\lambda v$ equates elements of different spaces; the compatible line $v_0=1$, $v_1=(1,0)$, $\lambda_0=1$ carries the same information and is well typed. | A mode is a compatibility relation between adjacent fibers, not an eigenvector, unless an identification is declared. | EVIDENCE_VERIFIED |
| CE-HERMITE-CORRELATED | `hermite-spectrum` if extended to correlated blocks | Jointly centered Gaussian coordinates with unit variances, common pairwise correlation $\rho>-1/(b-1)$, and unit-variance normalization give $\mathcal L_{b,\rho}e_k=b^{1-k/2}[1+(b-1)\rho]^{k/2}e_k$. Retaining the uncorrelated normalization instead makes the output law non-Gaussian, so the operator is not an endomorphism. | The scalar theorem needs independence and the unit-variance block statistic; correlation changes every eigenvalue. | EVIDENCE_VERIFIED |
| CE-HERMITE-MULTIVARIATE | `hermite-spectrum` if simple eigenvalues are claimed in dimension $d>1$ | For iid $N(0,I_d)$ and componentwise normalized sums, $\mathcal L_be_\alpha=b^{1-|\alpha|/2}e_\alpha$, so degree $k$ has multiplicity $\binom{d+k-1}{k}$. A general $\mathrm{GL}(d)$ action changes the reference covariance. | Simplicity and the gauge-free reading are scalar-case facts, not general ones. | EVIDENCE_VERIFIED |
| CE-BLOCK-COLLAPSE | any lower bound on the pushed score operator | A channel sending every block to one point has $\mathcal L_bh=0$ for every centered $h$, although the replicated score is nonzero. | The extensive budget is an upper bound with an exact defect; a fixed channel can annihilate the whole tangent. | EVIDENCE_VERIFIED |
| CE-FISHER-EQUALITY | `score-dqm-lift` if Fisher equality is read as experiment recovery | Independent $A,B$ with $\Pr_\theta(A=1)=\tfrac12+\tfrac\theta4$ and $\Pr_\theta(B=1)=\tfrac12+\tfrac{\theta^2}4$, channel retaining only $A$. At $\theta=0$ the $B$-score vanishes and equality holds, yet the conditional law of $B$ still moves with $\theta$. | Equality of Fisher information at one parameter is not sufficiency of the channel. | EVIDENCE_VERIFIED |
| CE-PROJECTED-FIXED-POINT | `beta-functions` if a retained fixed point is called exact | On $\mathbb R^2$ with $b=e$, $R(x,y)=(x,0)$ and $T(x,y)=(x,x)$ give $\beta^{\mathrm{ret}}(x,0)=(0,0)$ and $\beta^{\mathrm{ex}}(x,0)=(0,x)$. | Bounded idempotence of a retained projection is not closure; exactness requires exact-image invariance. | EVIDENCE_VERIFIED |
| CE-MOVING-COMPARISON | `beta-functions` if a beta is reported without its comparison scheme | On $\mathcal G_\ell=\mathbb R$ with $T_\ell=\operatorname{id}$ and $b_\ell=e$, the trivializations $J_\ell u=a_\ell u$ give $\beta_\ell(g)=(a_\ell/a_{\ell+1}-1)g$, nonzero for a moving frame. | Even an identity native step has a nonzero beta in a moving comparison frame; the scheme is part of the statement. | EVIDENCE_VERIFIED |
| CE-SCALE-INTERPOLATION | any continuous beta inferred from discrete endpoints | On the trivial line bundle, $\mathsf V^{(0)}(s,t)=1$ and $\mathsf V^{(\epsilon)}(s,t)=\exp\{\epsilon[\sin(2\pi s)-\sin(2\pi t)]\}$ satisfy the same two-parameter law and agree at all integer endpoints, with generators $0$ and $2\pi\epsilon\cos(2\pi s)$. | The smooth scale base, bundle, interpolation, and connection are additional declared data. | EVIDENCE_VERIFIED |
| CE-RAW-SCALE-DERIVATIVE | a coordinate derivative called a covariant scale beta | On the trivial line bundle with $g(s)=1$ parallel in a frame with $A_s=0$, the frame change $S_s=e^{s^2}$ gives raw derivative $2se^{s^2}\ne0$ while $A_s'=-2s$ and the covariant derivative vanishes. | A scale beta must transform by the connection-form law; a raw derivative does not. | EVIDENCE_VERIFIED |
| CE-MONODROMY | `fixed-objects` if a monodromy object is called one-step fixed | On $\mathbb R$, $F_0(x)=x+1$ and $F_1(x)=x-1$ have monodromy $F_1\circ F_0=\operatorname{id}$ fixing every point, while neither one-step map has a fixed point. | A periodic scheme has monodromy objects and $p$-cycles, not one-step fixed points. | EVIDENCE_VERIFIED |
| CE-TIER-TRANSFER | `fixed-objects` if fixedness transfers between tiers | Five witnesses: alternating references $(1/4,3/4)$ and $(3/4,1/4)$ at fixed $\mu=(1/2,1/2)$ swap the action; $H$ and $H+c$ share a class but not a mass; the projected witness above has a fixed retained but moving exact interaction; $S^1$ with the antipodal step and constant extraction fixes the law but not the configuration; an alternating receiver occupancy with a fixed row alternates the event law. | Each tier is tested in its own tier; no commuting injective bridge is available by default. | EVIDENCE_VERIFIED |
| CE-WEAK-LUMPABILITY | strong lumpability if inferred from one initial law | $\mathsf Y=\{1,2,3\}$, $c(1)=c(2)=a$, $c(3)=\beta$, with $1\mapsto3$ surely, $2\mapsto\{1,2\}$ uniformly, $3\mapsto\{1,3\}$ uniformly. Then $T(1,\{1,2\})=0\ne1=T(2,\{1,2\})$, yet from $\delta_3$ the chain stays in $\{1,3\}$ where $c$ is injective and the coarse process is Markov. | Strong lumpability is exactly the every-initial-law condition; a single initial law can be weakly lumpable without it. | EVIDENCE_VERIFIED |
| CE-MEMORY-NONNECESSITY | the resolved-class sufficient condition if called necessary | On $\mathbb R^2$ with $\mathsf C(x,y)=x$, $\mathsf P(w)=(w,0)$, and $T(x,y)=(x,x)$, one has $\mathsf QT\mathsf P w=(0,w)\ne0$ while $\mathsf CT\mathsf Q=0$, so every memory kernel vanishes and $w_{n+1}=w_n$ exactly. | Autonomy is vanishing of the total correction, not of the coupling operator. | EVIDENCE_VERIFIED |
| CE-HISTORY-COLLAPSE | `history-noncollapse` | A constant configuration coarse map $\mathsf R_\ell\equiv Q^\ast$ and zero coarse vector field $X_{\ell+1}\equiv0$ make both sides of the semiconjugacy equation vanish identically, so it holds for *every* positive factor while sending each nonconstant fine orbit to a point and every coarse Fisher duration to zero. On the coarse critical set the factor is undetermined and the requirement $a_\ell>0$ is vacuous. | Noncollapse and maximal-interval hypotheses are separate from semiconjugacy. The refutation is scope matched: it refutes "positive semiconjugacy implies a nonconstant shared history", not the flow identity, which remains true. | EVIDENCE_VERIFIED |
| CE-DURATION-MISMATCH | `history-duration-relation` | On $\mathbb R$, use the identity map and identical vector fields but metrics $dx^2$ and $4dx^2$. Semiconjugacy holds with factor one, there is no collapse, and the map is a diffeomorphism, while coarse Fisher duration is twice fine duration. The zero-information-loss realization is one context with an identity fiber map and channel weights $1$ and $4$. Separately, the identity parameter map from $\mathcal N(\mu,1)$ to $\mathcal N(\mu,\tfrac14)$ has Fisher difference $\Delta_F=1-4=-3$ and is a non-Markov Fisher-increase witness. | Fiber Fisher contraction does not identify configuration-space durations, because the two configuration metrics are separately declared data. The necessary and sufficient duration-ordering condition is the scalar inequality $\lVert T\mathsf R_\ell X_\ell\rVert_{\mathsf G_{\ell+1}}\leq\lVert X_\ell\rVert_{\mathsf G_\ell}$ along the orbit; the tensorial Loewner condition is sufficient and strictly stronger. | EVIDENCE_VERIFIED |
| CE-INDEPENDENT-ORBIT | `history-duration-relation` if a contraction theorem is read as comparing an independently recomputed coarse orbit | On $\mathbb R^2$ with $\mathsf R_\ell$ and the fiber map both the identity, so contraction is an equality and image durations agree, take $\Fenergy_\ell=\tfrac12(x^2+y^2)$ and $\Fenergy_{\ell+1}=\tfrac12(x^2+4y^2)$. The fine history from $(1,1)$ is $(e^{-t},e^{-t})$ of total length $\sqrt2\approx1.41421$; the independently recomputed coarse history is $(e^{-t},e^{-4t})$, of total length $\approx1.60023$, strictly greater. Semiconjugacy itself fails: $(-x,-y)=a(-x,-4y)$ forces $a=1$ and then $y=4y$. | A contraction theorem compares the *image* of the fine orbit, never an independently recomputed coarse orbit. This mechanism is logically independent of the metric-declaration mechanism of `CE-DURATION-MISMATCH`; neither implies the other. | EVIDENCE_VERIFIED |
| CE-ORIENTATION-REVERSAL | `history-semiconjugacy` if the factor is only required nonzero | On $\mathbb R$ with $\Fenergy_\ell=x^2/2$, $\Fenergy_{\ell+1}=-y^2/2$, and $\mathsf R_\ell(x)=-x$, the orbit sets agree and $T\mathsf R_\ell X_\ell=x$ while $X_{\ell+1}(\mathsf R_\ell x)=-x$, so $a\equiv-1$ and coarse descent becomes ascent. | Positivity of the factor carries the entire orientation content of the criterion; requiring only $a\neq0$ loses it. | EVIDENCE_VERIFIED |
| CE-PARTIAL-TRAVERSAL | any claim that semiconjugacy transfers completeness | $X_\ell=\partial_x$, $X_{\ell+1}=\partial_y$, $\mathsf R_\ell=\arctan$, so $a_\ell=(1+x^2)^{-1}>0$. Both flows are complete, yet $\Sigma_0=(-\pi/2,\pi/2)$ is a proper subinterval of the coarse maximal interval $\mathbb R$. | The general conclusion is the inclusion $\Sigma_Q\subseteq\bar J^{\max}$; equality requires $J_Q^{\max}=\mathbb R$ together with $\inf a_\ell>0$. | EVIDENCE_VERIFIED |
| CE-EQUAL-OBJECTIVE | `natural-gradient-semiconjugacy` if intertwining is inferred from equality of objectives | $\mathsf R_\ell=\mathrm{id}$ on $\mathbb R^2$, one objective $\tfrac12(x^2+2y^2)$, metrics $\operatorname{diag}(1,1)$ and $\operatorname{diag}(1,\kappa)$ with $\kappa\neq1$. Semiconjugacy would require $(x,2y)=a(x,2y/\kappa)$, forcing $a=1$ and then $\kappa=1$; it therefore fails on the dense open set $\{xy\neq0\}$. | The differential of an objective is metric free while its gradient is not. Sufficient conditions require functional compatibility plus horizontal conformality, giving $a_\ell=\chi_\ell'\varphi_\ell^2$. | EVIDENCE_VERIFIED |
| CE-AVG-NONCONTRACTION | any statement that a gauge-equivariant fiberwise averaging or variational coarse map loses information at the configuration tier | Two contexts, uniform disintegration $\kappa$, unit channel weights, $\Psi$ the identity so $\Delta_F^\Psi\equiv0$, centered normal fiber $\{\mathcal N(0,\Sigma)\}$ in the moment chart with $g^F(A,A)=A^2/(2\Sigma^2)$, configuration $(\Sigma_1,\Sigma_2)=(1,\delta)$, tangent $Z=(1,0)$, and the chart barycenter $\bar\Sigma=(\Sigma_1+\Sigma_2)/2$. The fine integrated metric is $\tfrac14$ and the coarse image metric is $1/(2(1+\delta)^2)$, so the ratio is $2/(1+\delta)^2$, equal to $\tfrac{20000}{10201}=1.9605920988\ldots$ at $\delta=10^{-2}$ and tending to the block size $2$ as $\delta\to0^+$. The defect $\Delta_{\mathrm{avg}}=\tfrac14-1/(2(1+\delta)^2)$ is negative exactly for $\delta<\sqrt2-1$ and tends to $-\tfrac14$. | Every other hypothesis of the exact averaging theorem holds on this datum: base-measure matching $f_\#\mu=\bar\mu$, unit weights, and an identity fiber map. Only chart joint convexity fails, and exactly so: the Hessian of $(\Sigma,A)\mapsto A^2/(2\Sigma^2)$ has determinant $-A^2\Sigma^{-6}<0$ for $A\neq0$, whereas the law-chart integrand $\dot p^2/p$ has Hessian determinant $0$ and trace $2/p+2\dot p^2/p^3>0$ and is jointly convex. Generic averaging-as-contraction is therefore retired; the replacement is the three-term exact defect under joint convexity. | EVIDENCE_VERIFIED |
| CE-MIXTURE-VS-CHART | identification of the mixture-tier Fisher defect with a chart-barycenter defect | Two contexts, uniform $\kappa$, unit-variance normal location fiber, $m_1(\theta)=\theta+1$, $m_2(\theta)=\theta-1$. The integrated fine configuration metric is $1$. The *mixture* $\bar P_\theta=\int p_{s_\theta(c)}\kappa(dc)$ has Fisher information $I_{\bar P}(0)\approx0.5504$, so its defect is $\approx0.4496$. The Gaussian *moment-chart* barycenter gives $\bar m(\theta)=\theta$ with Fisher $1$ and defect $0$. | The two coarse maps are different maps into different spaces and have different defects. The mixture tier additionally requires the coarse family to be closed under $\kappa$-mixtures, which fails for Gaussians; the chart tier requires joint convexity instead. Each carries its own instance of the exact averaging theorem and neither may be substituted for the other. | EVIDENCE_VERIFIED |
| CE-JOINT-BARYCENTER-NONMARKOV | any Markov predicate attached to a configuration arrow without exhibiting the kernel | There is no parameter-independent Markov kernel $T:\mathbb R^2\rightsquigarrow\mathbb R$ with $(\mathcal N(\mu_1,\Sigma_1)\otimes\mathcal N(\mu_2,\Sigma_2))T=\mathcal N(\tfrac{\mu_1+\mu_2}2,\tfrac{\Sigma_1+\Sigma_2}2)$ for all admissible parameters: letting $\sigma^2\to0^+$ in the equal-variance slice forces the characteristic function of $T(x,\cdot)$ to be $e^{it(x_1+x_2)/2}$, after which heat smoothing multiplies by $e^{-t^2\sigma^2/4}$ while the requirement demands $e^{-t^2\sigma^2/2}$. The centered-variance barycenter is realizable by $z=(x_1+x_2)/\sqrt2$ and the fixed-common-variance location barycenter by $z=(x_1+x_2)/2+\eta$ with $\eta\sim\mathcal N(0,\Sigma_0/2)$. | Markov realizability must be checked per map. It is a substantive hypothesis with content, not a typing remark, and it can be unsatisfiable for natural averaging maps. | EVIDENCE_VERIFIED |
| CE-COARSE-WEIGHTS | any cross-scale comparison of integrated configuration metrics with independently declared coarse weights | With zero horizontal defect, a genuine Markov fiber map, and $f_\#\mu=\bar\mu$, the two-channel difference is $\sum_x[w_x(h_x-f^*\bar h_x)+(w_x-(\bar w_x\circ f))f^*\bar h_x]$, which fails to be positive semidefinite as soon as $(\bar w_x\circ f)>w_x$ in some channel. | The declaration condition $\bar w_x\circ f\leq w_x$ is required for every integrated cross-scale comparison and follows from no contraction theorem. | EVIDENCE_VERIFIED |
| CE-BASE-MEASURE-REVERSAL | any cross-scale comparison of integrated configuration metrics without base-measure matching | Let $\mathcal C=\bar{\mathcal C}$ be the disjoint union of two copies of $\mathbb R$ with $f=\mathrm{id}$, let $h$ vanish on the first copy and equal $dx^2$ on the second, and let $\mu$ and $\bar\mu$ be unit point masses on the first and second copy respectively. With vanishing fiberwise defect and $h=f^*\bar h$ pointwise, the fine integrated tensor is $0$ and the coarse one is $1$. | The declaration condition $f_\#\mu=\bar\mu$ is required. **Typing note:** the witness must be stated on a positive-dimensional base; a two-point discrete set is a $0$-manifold with $\operatorname{Sym}^2T^*\mathcal C=0$, where no tangent direction exists and $h\equiv0$. | EVIDENCE_VERIFIED |
| CE-REPLICATION-NOT-MARKOV | any citation of independent $b$-fold replication as a Markov-contraction counterexample | $\{\mathcal N(x\mathbf 1_b,I_b)\}$ has Fisher information $b$ against the input family's $1$, but no parameter-independent normalized Markov kernel $K$ satisfies $\mathcal N(x,1)K=\mathcal N(x\mathbf 1_b,I_b)$ for every $x$: the conditional-variance defect would force $b\leq1$. | There is no coarse-graining arrow between the two families, so no data-processing statement is violated. The replication lift $\mathscr I_b$ has $\lVert\mathscr I_b\rVert=\sqrt b>1$ and is a change of reference tangent space; the composite $\mathscr L_b$ obeys $\lVert\mathscr L_b\rVert\leq\sqrt b$, not $\leq1$. The factor $\sqrt b$ belongs to the reference identification and to a declared configuration-metric normalization, and to nothing else. | EVIDENCE_VERIFIED |
| CE-ACTIVE-GAUGE-PULLBACK | `pullback-compatibility` if passive gauge covariance is read as active-gauge invariance | Trivial principal $\mathbb R$-bundle over $\mathbb R$ with the unit-variance normal location fiber, zero connection, and the zero section, so $h=0$. The bundle automorphism $F(u(x)g)=u(x)(x+g)$ carries the section to $\mathcal N(x,1)$ and $h$ to $dx^2$. | The pullback tensors are covariant under passive re-trivialization at fixed $(\omega,s)$; they are not fixed by the active gauge group at fixed connection, and they are not connection independent. | EVIDENCE_VERIFIED |
| CE-NO-PRINCIPAL-MAP | existence of an equivariant principal scale map, if asserted rather than declared | An equivariant $\mathcal P_\ell$ over $c_\ell$ exists if and only if $P_\ell\times_{\kappa_\ell}G_{\ell+1}\cong c_\ell^*P_{\ell+1}$. Taking the Hopf bundle over $S^2$ against the trivial bundle with $c_\ell$ the identity violates that condition. | This is a scope note on a datum admitted by `H-GAUGE`, not a refutation of any theorem; every statement using $\mathcal P_\ell$ inherits it. | EVIDENCE_VERIFIED |
| CE-NO-GLOBAL-SECTION | any global base semimetric asserted without a section-existence hypothesis | Over $S^2$ take the Hopf bundle with a von Mises fiber, so that the associated bundle is diffeomorphic to the total space $S^3$. Since $\pi_2(S^3)=0\neq\mathbb Z=\pi_2(S^2\times S^1)$, the bundle is nontrivial and admits no global section. | Without a global section there is no global $h_s^\omega$; section existence is a separate hypothesis. | EVIDENCE_VERIFIED |
| CE-TAU-ISOLATED-ZERO | any inversion of Fisher duration at a Fisher-null point | With $h_s=4x^2dx^2$ one gets $\tau(r)=\int_0^r2u\,du=r^2$, which is strictly increasing but whose inverse $\sqrt{\cdot}$ is not differentiable at $0$. A zero-speed subinterval destroys strict monotonicity outright. | Strict monotonicity of $\tau$ is not the same as being a regular coordinate; the clock stalls on null and critical segments. | EVIDENCE_VERIFIED |
| CE-COMPOSITE-ANOMALY-CANCELLATION | a claim that zero composite anomaly forces factorwise zero anomaly | In the abelian three-level instance with $f_{01}(x)=2x$, $f_{12}(y)=3y$, local connection forms $0,a_1dy,a_2dz$, and identity fiber maps, the composite anomaly is $2a_1+2(3a_2-a_1)=6a_2$. At $a_2=0$ and $a_1\ne0$, both factor anomalies are nonzero but their correctly typed terms cancel. | Composite vanishing is exactly $T^V\Psi_{12}A_{01}=-A_{12}$ on the pushed base vector; factorwise vanishing is sufficient, not necessary. | EVIDENCE_VERIFIED |
| CE-FINITE-TIER-GAUGE-SPAN | a finite configuration tier that claims constant mean-translation gauge orbits from an arbitrary basis | On $S^1$, take the one-dimensional basis span $\operatorname{span}\{\cos\theta\}$. It contains no nonzero constant mean field, so translating $s_\xi$ by any nonzero constant exits the displayed section family; with a repeated basis field, the coefficient map is additionally noninjective. | A finite additive-mean tier must declare weighted-$L^2$ basis independence and constant-field inclusion; the latter defines $\mathsf L_\ell$ and makes $\xi\mapsto\xi+\mathsf L_\ell g$ a genuine internal affine action. | EVIDENCE_VERIFIED |
| CE-INTEGRATED-CONFORMALITY | a pointwise conformality conclusion from finite-tier integrated conformality | Take a base with two equal-weight components, fine location fibers $\mathcal N(\mu,1)$, the shared constant tangent, and independent additive Gaussian noise of variances $3$ and $1/3$ on the two components. The resulting Markov pushforwards have pulled-back location Fishers $1/4$ and $3/4$. Their integrated ratio is $1/2$, but no common pointwise dilation exists. | The exact finite-tier criterion is the integrated bilinear/Gram identity. Pointwise recovery needs a declared compactly-supported-localizable tangent module and full-support measure (or another stated sufficient condition). | EVIDENCE_VERIFIED |
| CE-TAU-CHOSEN-PARAMETER | a claim that Fisher duration cannot be determined by an orbit parameter after choices | On the oriented unit-speed line with chosen origin $0$ and metric $dr^2$, the selected path has $\tau(r)=r$ for $r\geq0$. Rescaling the metric or moving the origin changes the duration representation without changing the unparameterized orbit. | $\tau$ may be a function of $r$ after metric, origin, orientation, and path choices, but it is not canonically determined by an unparameterized orbit or by scale depth. | EVIDENCE_VERIFIED |
| CE-L2-NONEXPONENTIAL | `score-dqm-lift` and `score-action-compatibility` if their domains are identified | For $\gamma=N(0,1)$ and $N_k(t)=\int e^{-t\mathrm{He}_k}d\gamma$: $N_1$ is finite for every $t$; $N_2(t)=e^t(1+2t)^{-1/2}$ is finite exactly for $t>-1/2$; for odd $k\geq3$, $N_k(t)=+\infty$ for every $t\neq0$; for even $k\geq4$, $N_k$ is finite exactly for $t\geq0$. Every $\mathrm{He}_k$ is nonetheless a two-sided quadratic-mean score. | Quadratic-mean score tangents strictly contain the bounded exponential-action directions, and no two-sided exponential chart contains a Hermite direction of degree three or higher. | EVIDENCE_VERIFIED |
| CE-MOVING-ATOM | `probability-common-domination` | The family $\mu_\theta=\tfrac12\delta_\theta+\tfrac12N(0,1)$ over an uncountable parameter set has no common sigma-finite dominating measure. | Mixed families need a fixed countable atomic support or a narrower parameter family. | CANDIDATE |
| CE-NONMEASURABLE-VERSION | `probability-measurable-version` | The version $p(o\mid X)=1+1_N(X)1_{\{X\}}(o)$ for a non-Borel $N$ integrates to the same probability law for each $X$ but is not jointly measurable. | Pointwise versions do not supply joint parameter-observation measurability. | CANDIDATE |
| CE-PARTITION-COFINALITY | `probability-measurable-version`; any proof that one generating sequence is cofinal among finite partitions | No finite dyadic partition of $[0,1]$ refines $\{C,C^c\}$ when $C$ is Borel but not a finite union of dyadic cells. | Kernel-KL convergence must use common-dominator conditional expectations, convex perspective, Fatou, and the log-sum upper bound. | CANDIDATE |
| CE-SIGMAFINITE-DECREASING | fixed sigma-finite integration proof if finite complements are not localized | Under counting measure on $\mathbb N$, $C_n=\{n,n+1,\ldots\}\downarrow\varnothing$ but $\nu(C_n)=+\infty$ for every $n$. | Partition the target into finite-measure pieces before using a Dynkin-system complement argument. | CANDIDATE |
| CE-TC-DIAGONAL | `correlated-collective-elbo-ledger`; any entropy-subtraction extension of total correlation | If $U\sim\operatorname{Unif}[0,1]$ and $Q=\operatorname{law}(U,U)$, both marginals are uniform but $Q\perp Q_1\otimes Q_2$, so $\mathrm{TC}(Q)=+\infty$. | The extended chain must be proved by nonnegative KL partition limits; finite differential-entropy algebra is only a corollary. | CANDIDATE |
| CE-H4-CANCELLATION | `extended-elbo`; expected-complete-log-likelihood acceptance outside H4 | On $\{2,3,\ldots\}$ take $q_n\propto[n(\log n)^2]^{-1}$ and a bounded nonconstant normalized tilt $p_1=q r$. Both $E_q\log q$ and $E_q\log p_1$ are $-\infty$, while the relative-log ELBOs are $0$ and $-\mathrm{KL}(q\Vert p_1)<0$. | The extended measure-level functional is primary; the classical split and complete-log M-coordinate require H4. | CANDIDATE |
| CE-INFINITE-DPI-EQUALITY | `extended-kl-data-processing`; finite-KL equality/recovery if extended to infinity | Let $K(a)=u$ and $K(b)=K(c)=v$, $P=(\delta_a+\delta_b)/2$, and $Q=(\delta_b+\delta_c)/2$. Fine and coarse KL both equal $+\infty$, but no reverse kernel recovers both. | Extended DPI holds, but $+\infty=+\infty$ carries no sufficiency or recovery conclusion. | CANDIDATE |
| CE-RCP-EXCEPTION | `extended-kl-data-processing` fixed-evidence corollary; `local-block-elbo-disintegration` at exceptional outside states | For independent uniform $(O,X)$, the uniform conditional kernel and the version changed to $\delta_0$ at $o=0$ are both RCPs, but a nonconstant channel gives different pushed values there. | Fix the fine measurable RCP as model data and declare the pushed version pointwise; joint laws determine conditionals only almost everywhere. | CANDIDATE |
| CE-BLOCK-EVIDENCE | `local-global-vfe-potential` if promoted to evidence ascent | An exact update of one conditional block generally leaves the recognition law different from the full posterior. | Block updates ascend the common collective objective; evidence ascent additionally requires a full exact old-posterior E phase. | CANDIDATE |
| CE-ATTENTION-CORRELATION | `attention-latent-label-elbo`; `correlated-collective-elbo-ledger` | A conditional label law with fixed row marginals but nonzero dependence has positive conditional total correlation. | Marginal row independence from the latent state does not justify a product-row ledger or independent row softmax. | CANDIDATE |
| CE-ATTENTION-EXTRA-RECORD | `attention-latent-label-elbo` without complete label exclusivity | Let $J\in\{0,1\}$ have prior $(1/2,1/2)$ and $D_0=D_1=0$, then add binary $R$ with $P(R=1\mid J=0)=1/4$ and $P(R=1\mid J=1)=3/4$. Given $R=1$, the displayed energy-only row is $(1/2,1/2)$ but Bayes gives $(1/4,3/4)$. | The complete selected-record likelihood must be label exclusive: its residual factor is label independent and no other record or factor reads $J$. | CANDIDATE |
| CE-OBSERVATION-DELETION | `observation-interaction-kernel-equivalence` if interpreted as deleting conditioning | For $Y\sim\operatorname{Bernoulli}(1/2)$ and deterministic message $O=Y$, conditioning gives $P(Y\mid O=o)=\delta_o$, while deleting $O$ returns the nondegenerate prior. | An environment-node redescription preserves the observation sigma-algebra; it does not erase informational conditioning. | CANDIDATE |
| CE-MIXED-JACOBIAN | `gauge-jacobian-scope` | On $\mathbb R$, let $\nu=\lambda+\delta_0$ and $R(y)=2y$. Then $R_\#\nu=\tfrac12\lambda+\delta_0$, so $d(R_\#\nu)/d\nu$ is $1/2$ off the atom but $1$ at the atom, not the universal determinant value $1/2$. | The measure pushforward and RN tier survive; restrict the determinant formula to the Euclidean Lebesgue or a separately declared relatively invariant tier. | EVIDENCE_VERIFIED |
| CE-NONCLOSED-GAUGE-FAMILY | `gauge-constraint-family` | For two design points, $\Theta=I$, $S=\operatorname{diag}(2,1)$, and $C=\begin{psmallmatrix}1&1\\0&1\end{psmallmatrix}$, the rechoices represented by $(I,I),(S,S)$ and $(C,I),(C,I)$ each preserve one shared link, while their pointwise product yields $C$ and $SCS^{-1}=\begin{psmallmatrix}1&2\\0&1\end{psmallmatrix}$. | The fixed-datum design-dependent set is a constraint-preserving family, not a group, until closure is proved. | EVIDENCE_VERIFIED |
| CE-ACTIVE-GAUGE-EVALUATION | `gauge-constraint-family` | On a connected base with trivial bundle and disconnected $G=\mathbb R^\times$, every smooth gauge function stays in one component. Pointwise diagonal values $1$ and $-1$ at two design points therefore lie in the abstract diagonal but not in the active-gauge evaluation image. | The active image is contained in the pointwise twisted diagonal; equality requires evaluation surjectivity and can fail through connectedness or global bundle constraints. | EVIDENCE_VERIFIED |
| CE-ZERO-SELF | `gaussian-positivity` | With every $A_i=0$ and $W_{ij}\succ0$ on a connected graph, $\ker\Lambda=\{\mathbf1_V\otimes v:v\in\mathbb R^K\}$, so the energy is PSD but its Lebesgue density is not normalizable. | A full-space Gaussian witness requires a trivial componentwise kernel; taking every self precision positive definite is a sufficient proper-prior construction. | EVIDENCE_VERIFIED |
| CE-IDENTITY-TWOCYCLE | `dobrushin-criterion`; phase-blind relevance labels | The identity channel makes every bounded mode marginal; a two-cycle has eigenvalue $-1$. | Dobrushin failure proves neither relevance nor marginality, and sign or complex phase cannot be erased. | EVIDENCE_VERIFIED |

The diagonal-product, horizontal-anomaly, section-descent, orbit-collapse, and duration-mismatch witnesses were absent from the initial favored narrative. The norm-distortion witness was reconstructed exactly and certified earlier. The Task 10 register now includes the independently reconstructed original witnesses and the later repairs: nonsmooth descent, base-cocycle residual, independent orbit, orientation reversal, partial traversal, equal objective, averaging noncontraction, mixture versus chart, nonrealizable joint barycenter, coarse-weight reversal, base-measure reversal, replication-is-not-Markov, active-gauge pullback, absent principal scale map, absent global section, isolated Fisher-null zero, composite-anomaly cancellation, finite-tier gauge-span failure, integrated-conformality failure, and chosen-parameter duration. This deliberately avoids a stale aggregate total; the rows above are authoritative.

Two arithmetic corrections were made against prior Task 10 artifacts and are recorded here rather than silently absorbed. First, the exact coarse-to-fine ratio in `CE-AVG-NONCONTRACTION` is $20000/10201$, not $5000/10201$; the decimal value $1.9605921$ reported in the interface-reconciliation artifact is correct, but the fraction printed beside it is not. Second, the Hessian determinant of the Gaussian moment-chart Fisher integrand is $-A^2\Sigma^{-6}$, not $-4A^2\Sigma^{-6}$; the sign, and therefore every conclusion drawn from it, is unaffected. Both were recomputed symbolically during the Task 10 integration pass.

Three of the newly certified witnesses refute a reading that appeared in earlier Task 10 material rather than in the manuscript: generic averaging as a configuration-tier contraction, the convention-mixed base-cocycle correction, and the degree-two Hermite direction as a witness for absence of a two-sided exponential-action neighborhood. Each replacement is recorded in the row it repairs.
