<!-- rigorous-theory-search-metadata {"contract_id":"contract-sha256-b6f7aeefd57a2cac70495af4744dd3e3f58dccfe630d6407e3e277f34dac526b","schema_version":"rigorous-theory-search/v1","target_digest":"b6f7aeefd57a2cac70495af4744dd3e3f58dccfe630d6407e3e277f34dac526b"} -->
# Counterexample register

This checkpoint records quantifier-matched candidate falsifiers. Each remains uncertified until an independent derivation reproduces its hypotheses and calculation. Failure to find another witness is not supporting evidence.

| ID | Claims attacked | Typed witness and decisive equation | Assumption boundary | Status |
| --- | --- | --- | --- | --- |
| CE-ACTION-LP | `action-analyticity` if enlarged to an arbitrary finite-$L^p$ neighborhood | Under a Gaussian reference, an unbounded finite-$L^p$ function such as $\varphi=-x^2$ has $e^{-\varphi}=e^{x^2}$ nonintegrable. | The nonlinear chart must remain an open bounded $L^\infty$ chart unless a stronger uniform-moment theorem is proved. | CANDIDATE |
| CE-CIRCLE-NORM | `circle-norm-witness`; every topology-free relevance statement | For $Uf=f\circ D$ with $D(x)=2x\bmod1$, Haar preservation gives $r_{L^\infty}(U)=1$, while $[f\circ D^n]_\alpha\leq2^{\alpha n}[f]_\alpha$ and a fixed Fourier mode give $r_{C^\alpha}(U)=2^\alpha$. | The operator formula and measure are identical; only the declared topology changes. | CANDIDATE |
| CE-PRODUCT-DIAGONAL | `interaction-gauge-domination`; automatic availability of `hoeffding-inverse` after arbitrary law-level coarse-graining | Push Lebesgue law on $[0,1]$ through $x\mapsto(x,x)$. The output is supported on the diagonal, while every product of two compatible nonatomic marginals assigns that diagonal mass zero. | Exact law pushforward survives; product-reference equivalence must be a separate per-scale premise or theorem. | CANDIDATE |
| CE-WEIGHTED-FRECHET | weighted-one-space unification of `essential-spectrum` with `action-analyticity` | On $c_0(5^n)$, the advance-or-reset reverse operator has $U\varphi=2\varphi$ for $\varphi_n=1/3+(2/3)4^n$, yet weighted-norm spikes make $\|Q(f)-Uf\|_w/\|f\|_w$ fail to tend to zero. | A relevant bounded linear operator need not be the Frechet derivative of the nonlinear logarithmic action map on that weighted chart. | CANDIDATE |
| CE-HORIZONTAL-ANOMALY | `pullback-compatibility` without `horizontal-defect-anomaly` | On a trivial line bundle, use the identity bundle map, source horizontal field $\partial_x$, target field $\partial_x+a\partial_y$, and the zero section. The source base pullback is zero and the target is $a^2dx^2$. | Base Fisher positivity requires zero horizontal defect; otherwise all signed anomaly terms remain. | CANDIDATE |
| CE-SECTION-DESCENT | `configuration-projectability` | Collapse $S^1$ to one point, use identity fiber map, and take $Q(x)=\sin x$. No section over the point can satisfy $\Psi Q=\bar Q f$. | A bundle morphism does not induce a configuration map on all section spaces. | CANDIDATE |
| CE-TRIVIALIZATION | `generalized-modes` without full tempered comparison control | Let every derivative be the identity on $\mathbb R$ but equip level $\ell$ with norm $e^{\ell^2}|x|$. Apparent growth becomes superexponential. | Tempering only scalar mode representatives is insufficient; the full comparison trivialization and inverse must have subexponential distortion relative to block scale. | CANDIDATE |
| CE-HISTORY-COLLAPSE | `history-noncollapse` | A constant configuration coarse map and zero coarse vector field satisfy positive semiconjugacy for every fine vector field while sending each nonconstant fine orbit to a point. | Noncollapse and maximal-interval hypotheses are separate from semiconjugacy. | CANDIDATE |
| CE-DURATION-MISMATCH | `history-duration-relation` | On $\mathbb R$, use the identity map and identical vector fields but metrics $dx^2$ and $4dx^2$. Semiconjugacy holds with factor one while coarse Fisher duration is twice fine duration. | Fiber Fisher contraction does not identify configuration-space durations. | CANDIDATE |
| CE-L2-NONEXPONENTIAL | `score-action-compatibility` if its domains are identified | A centered $L^2$ variable may have an infinite moment-generating function for every positive parameter, yet the quadratic DQM path still realizes it as a score. | DQM score tangents are larger than bounded exponential-action directions. | CANDIDATE |
| CE-MOVING-ATOM | `probability-common-domination` | The family $\mu_\theta=\tfrac12\delta_\theta+\tfrac12N(0,1)$ over an uncountable parameter set has no common sigma-finite dominating measure. | Mixed families need a fixed countable atomic support or a narrower parameter family. | CANDIDATE |
| CE-NONMEASURABLE-VERSION | `probability-measurable-version` | The version $p(o\mid X)=1+1_N(X)1_{\{X\}}(o)$ for a non-Borel $N$ integrates to the same probability law for each $X$ but is not jointly measurable. | Pointwise versions do not supply joint parameter-observation measurability. | CANDIDATE |
| CE-MIXED-JACOBIAN | `gauge-jacobian-scope` | A continuous-plus-atomic reference need not transform by the Lebesgue determinant factor even though its measure-level pushforward remains defined. | Restrict the determinant formula to a relatively invariant reference tier. | CANDIDATE |
| CE-NONCLOSED-GAUGE-FAMILY | `gauge-constraint-family` | The referee's explicit $GL(2,\mathbb R)$ shared-link rechoices each preserve the constraint while their product does not. | The design-dependent set is a constraint-preserving family, not a group, until closure is proved. | CANDIDATE |
| CE-ZERO-SELF | `gaussian-positivity` | With $A_i=0$, the interaction precision retains the consensus nullspace, so the quadratic kernel can be semidefinite without defining a normalized full-space Gaussian density. | A constructive probability witness requires positive-definite self precision. | CANDIDATE |
| CE-IDENTITY-TWOCYCLE | `dobrushin-criterion`; phase-blind relevance labels | The identity channel makes every bounded mode marginal; a two-cycle has eigenvalue $-1$. | Dobrushin failure proves neither relevance nor marginality, and sign or complex phase cannot be erased. | CANDIDATE |

The diagonal-product, horizontal-anomaly, section-descent, norm-distortion, orbit-collapse, and duration-mismatch witnesses were absent from the initial favored narrative and therefore remain priority attacks for later independent reconstruction.
