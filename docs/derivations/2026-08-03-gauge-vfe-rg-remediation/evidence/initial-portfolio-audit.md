# Initial mechanism-diverse portfolio audit

Contract: `contract-sha256-b6f7aeefd57a2cac70495af4744dd3e3f58dccfe630d6407e3e277f34dac526b`.

This record preserves the two isolated-context discovery passes after both had completed. They share one Codex runtime, model family, and repository, so their agreement is not proof or independent corroboration. Neither pass edited the proof artifacts.

## Operator and probability pass

The pass returned seven concrete candidate entries. Its bounded-action lemma reconstructed

$$
Q_\ell(\varphi)=-\log\mathbb E[e^{-\varphi}\mid Z],
\qquad
DQ_\ell(0)\varphi=\mathbb E[\varphi\mid Z],
$$

and the negative conditional-covariance Hessian, quotient descent, $L^p$ contraction, and Fisher defect. Its DQM lemma supplied the normalized squared-linear two-sided path and the extensive score norm $\|E_bh\|_2=\sqrt b\|h\|_2$. Its Hoeffding lemma expanded the commuting-coordinate identity and exposed the finite bound $\|P\|\leq3^{|V|}-1$. Its Gaussian construction obtained $L_be_k=b^{1-k/2}e_k$.

The same pass supplied three decisive adverse seams: the diagonal embedding destroys equivalence to every compatible nonatomic product reference; the weighted reset operator admits a relevant eigenfunction while its nonlinear logarithmic action map is not Frechet differentiable in the same weighted norm; and circle doubling has spectral radius one on $L^\infty$ but $2^\alpha$ on $C^\alpha$.

## Geometry and cocycle pass

The pass returned fourteen typed candidate lemmas, a dependency graph, and ten falsifiers. It proved at candidate level that associated-bundle descent requires principal equivariance and fiber intertwining; Markov kernels and smooth induced law-manifold maps are different objects; covariant first jets acquire a horizontal-defect anomaly; vertical Fisher defects compose additively; and complete comparison trivializations, not only scalar modes, require tempered distortion.

The pass also showed that section descent can fail for $S^1\to\{*\}$ with $Q(x)=\sin x$, positive semiconjugacy can collapse all nonconstant fine histories, and identical dynamics with metrics $dx^2$ and $4dx^2$ have unequal Fisher durations.

## Convergence and redirects

Conditional expectation recurred in both action and score families, but their nonlinear and DQM domains remain distinct. DQM and Hermite returns converged, so further scalar Gaussian work is redirected to correlated, multivariate, or gauge-mixed blocks. Weighted and circle witnesses converged on norm dependence, so duplication is redirected to the unresolved existence of a weighted nonlinear chart with finite relevant spectrum and two-sided evidence control. Hoeffding inversion and category typing converged on the underexplored product-domination seam, which is the highest-priority redirect.

## Reconciliation

The frozen target now treats product-reference equivalence as a per-scale premise rather than a consequence of Markov positivity. It release-gates full-trivialization tempering, section projectability, noncollapse, and the separate Fisher-speed relation. The geometry pass found that the approved design reuses the configuration coarse-map glyph already used for a reference-space endomorphism; later manuscript implementation therefore needs a recorded design amendment before renaming. Its warning about a definition sign does not apply to the current design snapshot, whose semiconjugacy display already uses equality.
