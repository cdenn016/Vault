<!-- rigorous-theory-search-metadata {"contract_id":"contract-sha256-b6f7aeefd57a2cac70495af4744dd3e3f58dccfe630d6407e3e277f34dac526b","schema_version":"rigorous-theory-search/v1","target_digest":"b6f7aeefd57a2cac70495af4744dd3e3f58dccfe630d6407e3e277f34dac526b"} -->
# Construction or strongest theorem

No mathematical claim is certified at this Task 1 checkpoint. The strongest candidate package is the following conditional finite-network construction, which later tasks must prove one atomic claim at a time.

For each admitted scale, the measure-pair pushforward defines the normalized reverse-kernel action increment

$$
Q_\ell(\varphi)(z)
=-
\log\mathbb E_{\Pi_\ell(\cdot\mid z)}e^{-\varphi}.
$$

On an open bounded $L^\infty$ chart, the candidate derivatives are

$$
DQ_\ell(0)\varphi=\mathbb E[\varphi\mid Z],
\qquad
D^2Q_\ell(0)[\varphi,\psi]
=-
\operatorname{Cov}(\varphi,\psi\mid Z).
$$

The middle map is expected to contract every normalized $L^p$ tangent and to satisfy the exact $L^2$ Fisher-defect identity. That candidate theorem does not classify extensive interaction relevance by itself.

At a finite interaction scale whose normalized law is equivalent to the declared covariant product reference, full power-set Hoeffding extraction is expected to invert assembly modulo constants. The exact candidate coupling derivative is

$$
M_\ell(g_\ell)
=P_{\ell+1}\overline U_\ell(E_\ell g_\ell),
$$

while a retained projection is separately

$$
M_\ell^Q(g_\ell)=\mathsf Q_{\ell+1}M_\ell(g_\ell)
$$

and carries the omitted quotient residual. The diagonal embedding counterexample proves that equivalent product reference structure must be assumed or proved at every scale; it is not a consequence of Markov positivity.

For centered DQM scores, the proposed two-sided realizing path is

$$
\frac{d\mu_t}{d\mu_*}
=
\frac{(1+th/2)^2}{1+t^2\lVert h\rVert_2^2/4}.
$$

At the iid Gaussian fixed law with $Z=b^{-1/2}\sum_iX_i$, the candidate Mehler/Hermite theorem is

$$
L_be_k=b^{1-k/2}e_k,
$$

which inhabits relevant, marginal, and irrelevant centered score modes without contradicting normalized Fisher contraction.

The strongest candidate history statement is conditional: if explicit regular configuration manifolds, strong configuration Fisher metrics, locally well-posed VFE vector fields, a smooth noncollapsing configuration coarse map, and the positive semiconjugacy equality are constructed, then fine integral curves map locally to coarse integral curves by an orientation-preserving reparameterization. Their Fisher durations obey separate speed formulas and are not generally equal.

Certification still requires direct derivations, source-hypothesis mappings, artifact binding, independent reconstruction, adversarial response, and oracle erasure. The 61 ledger entries remain `CANDIDATE`, so this checkpoint licenses no manuscript theorem status.
