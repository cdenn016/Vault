# 2026-07-17 Edits

## VFE 4.0 Gauge-Causal ELBO White Paper

Added `docs/superpowers/specs/2026-07-17-vfe4-gauge-causal-elbo-white-paper-design.md`, the approved design specification for a publication-track VFE 4.0 white paper. The specification defines the causal generative joint, structured posterior, principal and associated bundle construction, exact ELBO boundary, transformer correspondence, falsification program, and V3/V4 separation.

The design adopts a dual-coordinate Gaussian architecture. Joint information parameters `(h, J)` are the canonical sparse inference representation; expectation coordinates `(mu, M)` and the moment view `(mu, Sigma)` remain derived interfaces. The specification records the associated gauge laws, positive-definiteness requirements, full joint entropy, nonconjugate categorical-emission boundary, and the rule that Euclidean optimization in natural coordinates is not automatically a natural gradient.

No manuscript prose, bibliography entry, figure, wiki page, immutable source note, or existing manuscript was changed in this design-only step.

Added `docs/superpowers/plans/2026-07-17-vfe4-gauge-causal-elbo-white-paper.md` after design approval. The plan decomposes the manuscript into twelve reviewable tasks covering the document shell, citation verification, observed-data statement, bundle geometry, causal joint, structured information-form posterior, shared ELBO derivation, transformer crosswalk, figures, rigor review, artifact verification, and WIP-preserving publication lifecycle.

## Verification

Checked the specification for placeholders, banned wording, banned LaTeX spacing macros, internal scope conflicts, and whitespace errors. Specialist review separated posterior-family ablation from the zero-transition-coupling control, rejected temporal shuffling of real text as a decisive causal diagnostic, and restricted monotonicity claims to exact coordinate, valid majorize-minimize, or explicitly accepted generalized-EM updates.

Created the reproducible VFE 4.0 white-paper shell at `manuscripts/VFE4_gauge_causal_elbo_whitepaper.tex`, with stable section inputs under `manuscripts/vfe4_whitepaper/` and the locally copied `scientific_report.sty` asset. The shell fixes the approved title metadata and proof-dependency order without adding citations, figures, derivations, or empirical claims.

The required smoke build was run from `manuscripts` after setting `$env:WINDIR = $env:SystemRoot` for the PowerShell process: `latexmk -lualatex -interaction=nonstopmode -halt-on-error -outdir=C:\tmp\vfe4-whitepaper-build-20260717 VFE4_gauge_causal_elbo_whitepaper.tex`. It exited 0, reported that all targets were up to date, and produced `C:\tmp\vfe4-whitepaper-build-20260717\VFE4_gauge_causal_elbo_whitepaper.pdf`. The environment assignment is necessary because the TeX Live `latexmk` wrapper otherwise reads an unset `WINDIR` value before LuaLaTeX starts.

Verified the reusable bibliography keys `AmariNagaoka2000`, `Blei2017`, `Bishop2006`, `Kingma2013`, `Wainwright2008`, `Neal1998`, `Vaswani2017`, `chung2015recurrent`, `fraccaro2016sequential`, `krishnan2017structured`, `cohen2019gauge`, and `Friston2010` against their primary publisher, proceedings, or arXiv records. The sources were [AMS](https://bookstore.ams.org/mmono-191/), [Taylor & Francis](https://www.tandfonline.com/doi/abs/10.1080/01621459.2017.1285773), [Springer](https://link.springer.com/book/9780387310732), [arXiv:1312.6114](https://arxiv.org/abs/1312.6114), [Foundations and Trends](https://doi.org/10.1561/2200000001), [Springer chapter DOI](https://link.springer.com/chapter/10.1007/978-94-011-5014-9_12), [NeurIPS 2017](https://proceedings.neurips.cc/paper_files/paper/2017/hash/3f5ee243547dee91fbd053c1c4a845aa-Abstract.html), [NeurIPS 2015](https://proceedings.neurips.cc/paper/2015/hash/b618c3210e934362ac261db280128c22-Abstract.html), [NeurIPS 2016](https://proceedings.neurips.cc/paper/2016/hash/208e43f0e45c4c78cafadb83d2888cb6-Abstract.html), [AAAI 2017](https://ojs.aaai.org/index.php/AAAI/article/view/10779), [PMLR](https://proceedings.mlr.press/v97/cohen19d.html), and [Nature Reviews Neuroscience](https://doi.org/10.1038/nrn2787).

Added the absent keys `archer2015black`, `johnson2016composing`, `bowman2016generating`, `weiler2021coordinate`, `hu2022fuse`, `burda2015importance`, and `maddison2017filtering` using the verified records [arXiv:1511.07367](https://arxiv.org/abs/1511.07367), [arXiv:1603.06277](https://arxiv.org/abs/1603.06277), [arXiv:1511.06349](https://arxiv.org/abs/1511.06349), [arXiv:2106.06020](https://arxiv.org/abs/2106.06020), [ACL Anthology 2022.naacl-main.51](https://aclanthology.org/2022.naacl-main.51/), [arXiv:1509.00519](https://arxiv.org/abs/1509.00519), and [arXiv:1705.09279](https://arxiv.org/abs/1705.09279). These records provide the exact author ordering, titles, years, venues, pages, and ACL DOI required by the white paper.

The command `rg -n '^@' manuscripts/references.bib` exited 0. A complete parser scan found 412 entries with zero exact duplicate keys and zero case-folded duplicate keys. With a temporary `\nocite{archer2015black,johnson2016composing,bowman2016generating,weiler2021coordinate,hu2022fuse,burda2015importance,maddison2017filtering}` line, the smoke command `$env:WINDIR = $env:SystemRoot; latexmk -lualatex -interaction=nonstopmode -halt-on-error -outdir=C:\tmp\vfe4-whitepaper-citation-build-20260717 VFE4_gauge_causal_elbo_whitepaper.tex` exited 0. BibTeX read `references.bib`, reported no repeated-entry or missing-entry warnings, and its warning counter was zero. The temporary `\nocite` line and task-owned build artifacts were removed after the check.

## VFE 4.0 Figure Remediation

Revised the graphical abstract after semantic review. Stage 2 now displays the declared causal order as one-way solid dependencies from observed history to model state and from model state to state, together with the direct observed-history-to-state dependency. The former bidirectional dashed state-model implication was removed. Stage 4 now names the observation contribution as an expected categorical log likelihood rather than classification accuracy, and the objective box reads `expected log likelihood - complexity`. The final 1825 by 862 pixel RGB PNG has SHA-256 `B5AFDF06FE4F3DA48C603627B0E0BBC525AF743F3473708D3BB2B5C352F50B5A`.

Revised the generative-model figure so each causal parent bank and categorical selector is a distinct co-parent of its receiving variable. The selected-value intermediates were removed because they visually serialized dependencies that are joint in the declared kernels. The single fixed geometric object `Gamma` now lies outside the time plate and has separate conditioning arrows to the model transition, state transition, and categorical emission.

The standalone factor graph compiled with pdfLaTeX and produced a one-page 73,306-byte PDF. The complete command `$env:WINDIR=$env:SystemRoot; latexmk -lualatex -bibtex -interaction=nonstopmode -halt-on-error -outdir=C:\tmp\vfe4-task9-remediation-build-20260718 VFE4_gauge_causal_elbo_whitepaper.tex` exited 0 and produced a 64-page, 1,693,130-byte PDF. The final log contained no hard error, undefined control sequence, overfull box, underfull box, undefined reference, undefined citation, multiply defined label, or rerun warning. Physical pages 6 and 19 were rasterized at 160 dpi and inspected at original resolution; the revised text, causal arrows, global-geometry placement, captions, and page boundaries were legible without clipping.

## VFE 4.0 Integrated Rigor Pass

Integrated the complete pre-panel draft and enforced one notation path across the manuscript. State variables remain $z$, model variables remain $m$, source assignments remain $(a,b)$, and deterministic geometry remains $\Gamma$. Gaussian information coordinates are $(h,J)$, the exact Fisher-dual expectation coordinates are now consistently written as $\xi=(\mu,M)$, and $(\mu,\Sigma)$ remains the moment view. This change removes the former collision between the expectation-coordinate symbol and the source-prior temperature $\tau$. The notation appendix now provides a two-page mapping of the primary objects, domains, coordinate conversions, and objective symbols.

The executive summary now mirrors the three construction limits established in the main text: deterministic $\Gamma$ does not define a normalizable latent law over a noncompact gauge volume, the categorical softmax emission is nonconjugate to the conditional Gaussian family, and exact source marginalization produces a mixture whose component count grows with the assignment space. The proposition-status table now names the group, representation, support, measurability, integrability, and invertibility assumptions that delimit its claims. The latent-geometry discussion replaces an unspecified Jacobian with the Jacobian induced by a declared gauge-fixing or quotient map.

The falsification program now fixes the first categorical vocabulary to $|\mathcal X|=3$, specifies the H1 analytic reference problem as $T=2$, $d_z=d_m=1$ with positive source priors and both admissible parents at $t=2$, states the full source-enumeration count, and ties stochastic changes to estimated Monte Carlo error. The remaining planning sentence in the proof appendix was converted to completed prose. A manuscript-wide rigor scan returned 51 candidates; contextual review retained only mathematical permissions, explicit hypotheses, null-set qualifications, scoped omissions, and other non-defective uses. The initial project wording pattern found no hits for the phrases it covered, UK spellings, banned LaTeX spacing macros, placeholders, prose horizontal rules, or unsupported first-person claims; it did not yet cover the later-detected phrase `in particular`. The graphical-abstract caption continues to name the observation sector as an expected categorical log likelihood.

The integrated command `$env:WINDIR=$env:SystemRoot; latexmk -lualatex -bibtex -interaction=nonstopmode -halt-on-error -outdir=C:\tmp\vfe4-task10-integration-build-20260718 VFE4_gauge_causal_elbo_whitepaper.tex` exited 0. The LuaLaTeX log reports a 66-page, 1,711,232-byte PDF and contains zero hard errors, undefined control sequences, undefined references or citations, multiply defined labels, rerun warnings, overfull boxes, or underfull boxes. The integrated draft is review-ready but remains uncommitted so the independent information-geometry, gauge-geometry, variational-inference, transformer-language-modeling, numerical-analysis, and adversarial-scope panel can review one stable diff before Task 10 corrections and commit.

## VFE 4.0 Specialist-Panel Reconciliation

Applied every correction accepted by the independent specialist panel. The ordinary evidence ELBO is now separated from the proper generalized-Bayes loss posterior, whose finite normalizer and variational characterization are stated with a citation to Bissiri, Holmes, and Walker. The language-model observation chapter now derives the target-blind prefix filter, held-out token conditional, and normalized Bayes update explicitly. Exact enumeration and integration, declared sequential Monte Carlo or importance estimators, and free-running ancestral generation are treated as three distinct procedures. The paper consistently describes realized token occurrences as observations.

Reclassified H1, H2, H5, and H7 as implementation verification obligations, H3 as a synthetic structured-posterior adequacy test, and H4, H6, and H8 as empirical hypotheses. H1 through H8 remain protocol templates until the dataset or synthetic law, split, tokenizer, estimator, exact seed identifiers, compute budget, stopping rule, effect threshold, interval rule, and failed-run policy are frozen. H6 now requires six matched arms: conventional autoregressive cross-entropy, an ordinary latent sequence model, an identity-frame gauge-free control, a source-free control, a model-channel-free control, and full VFE 4.0. Teacher-forced held-out likelihood and free-running rollout diagnostics are separate endpoints.

Strengthened the finite-precision boundary without changing the exact-arithmetic theorems. H1 and H2 use deterministic float64-or-higher references or declared rounding and estimator-error budgets. Probability oracles require stable `logsumexp` and `log_softmax`, positive-prior masked support, and hard failure for an all-invalid routing row. H7 uses float64, a declared frame and operand condition envelope, absolute, relative, and backward residuals, and separately labeled out-of-envelope stress tests. It verifies implementation covariance only; attribution of predictive benefit to gauge structure requires the matched gauge-free H6 intervention. Exact positive-definite factor assembly is retained, while float32 implementations must use checked square-root or Cholesky assembly or higher-precision accumulation, condition and pivot diagnostics, and a declared reject, rescale, or regularize policy. The pure update path now uses SPD convex damping for conjugate targets and checked whitened-coordinate backtracking with complete-ELBO acceptance for general proposals.

Clarified the remaining approximation and geometry boundaries. Global moment matching of a source mixture generally makes both covariance and precision dense, so a sparse path must request selected block marginals or declare a sparse information projection and report its error. A raw coercive prior on noncompact frame coordinates normally gauge-fixes or breaks the redundancy; preserving the redundancy requires a proper quotient law or a declared gauge-fixing map and induced Jacobian. The Fisher identity is restricted to a conditional Gaussian component; the complete labeled law also has a categorical-simplex block and source-weighted component blocks, while the source-marginal mixture has no implied componentwise global dual chart. Fixed source priors define normalized mixture ancestry rather than content-dependent transformer attention. The subtitle now identifies a structured language model, Chapter 8 is a transformer-inspired structural crosswalk, and the executive claim is limited to a jointly normalized and auditable construction.

Recorded the panel's downgraded or rejected findings without converting them into stronger claims. The H7 condition-domain and float32 positive-definiteness findings were retained as medium implementation and promotion-protocol defects rather than failures of the exact gauge or factor-assembly theorems. Content-dependent generative attention remains a future normalized extension rather than a present defect. An unfrozen H6 protocol is a preregistration boundary, not evidence against the theory. Independent recomputation confirmed no defect in Gaussian coordinate conversion, Legendre and Fisher duality, the normalized joint and mixed entropy, the KL decomposition, typed associated bundles, Regime I cocycle and flat holonomy, or complete-objective gauge invariance.

The graphical abstract was edited with the built-in image-generation tool using this exact prompt:

```text
Use case: precise-object-edit
Asset type: publication white-paper graphical abstract
Input image: edit target; preserve the full existing five-stage scientific infographic.
Primary request: Edit ONLY the probability bar chart inside Stage 5, the rightmost panel titled "Prior-based next-token generation". Replace the currently non-normalized-looking bars with a visibly normalized categorical probability vector. Show four named bars v_1, v_2, v_3, v_V with heights exactly 0.32, 0.24, 0.16, and 0.13; retain an ellipsis between v_3 and v_V; add a compact clear annotation inside Stage 5 reading "omitted mass = 0.15". These displayed probabilities plus omitted mass total exactly 1.00. Keep the y-axis labeled from 0.0 to 1.0 and keep the formula "p (next token | context)".
Constraints: Change only Stage 5's bar chart and its omitted-mass annotation. Preserve all other pixels and approved semantic invariants as closely as possible: exact five-stage horizontal layout, panel numbering and borders, all causal arrows in Stage 2, all text and symbols in Stages 1-4, ELBO wording, sparse precision matrix, posterior contour, test badge, navy/teal/orange palette, line weights, typography, bottom legend, white background, and original 1825:862 aspect ratio. Keep Stage 5's title and the orange dashed "test" badge unchanged. No added panels, no removed arrows, no new legend items, no watermark.
Avoid: changing any wording outside the Stage 5 probability chart; changing the probability values; bars whose visible sum exceeds 0.85; omitted-mass ambiguity; cropping; warped text.
```

The resulting RGB image is 1825 by 862 pixels, 1,159,300 bytes, with SHA-256 `804D60C4CC4C45657F37E2294DDF36A3C73D8FB9518265822E7987682604FF00`. Original-resolution inspection confirmed the four displayed values, omitted mass, five-stage layout, causal arrows, colors, legend, and unmodified Stage 1 through Stage 4 semantics.

The final command `$env:WINDIR=$env:SystemRoot; latexmk -lualatex -bibtex -interaction=nonstopmode -halt-on-error -outdir=C:\tmp\vfe4-task10-panel-final-build-20260718 VFE4_gauge_causal_elbo_whitepaper.tex` exited 0 and produced `C:\tmp\vfe4-task10-panel-final-build-20260718\VFE4_gauge_causal_elbo_whitepaper.pdf`, a 68-page, 1,748,853-byte PDF with SHA-256 `6065205A185182008C690BE102465E4371053D0EF492E633FDDD2271E0B69082`. Its final log has zero hard errors, undefined references or citations, rerun requests, overfull boxes, or underfull boxes. The only remaining warnings are inherited shell warnings: one ignored `inputenc` warning, 49 `fancyhdr` head-height warnings, and one duplicate front-matter `page.1` destination warning. Physical pages 1, 7, 9, 10, 18, 26, 30, 34, 38, 41, and 48 through 56, together with notation and bibliography pages 66 through 68, were rendered at 160 dpi and inspected; the edited equations, tables, diagrams, headings, and page boundaries are legible and unclipped.

The final rigor scan reported 52 contextual candidates: 36 epistemic, 2 vagueness, 3 hand-waving, 5 scope, 1 analogy, 1 quantifier, and 4 magnitude candidates. Manual triage found no load-bearing hedge; the additional scope hit is the explicit graphical-abstract omitted-mass annotation. A final expanded stock-phrase scan detected `In particular, noncompact` in the latent-geometry discussion; that sentence was rewritten as a direct causal statement. The rerun, which explicitly included `in particular`, found zero banned stock phrases. The American-English, banned LaTeX spacing, placeholder, prose-horizontal-rule, and first-person-prose scans also returned zero, and `git diff --check` reported no whitespace error.

## VFE 4.0 Final Artifact Verification

Performed the final verification from an empty task-owned output directory. After setting `$env:WINDIR=$env:SystemRoot`, the command `latexmk -lualatex -bibtex -interaction=nonstopmode -halt-on-error -outdir=C:\tmp\vfe4-whitepaper-build-20260717 VFE4_gauge_causal_elbo_whitepaper.tex` ran from `manuscripts` and exited 0. `pdfinfo` reports 68 letter-size pages and 1,748,672 bytes. The final PDF SHA-256 is `037B5DC7B1D9239642123D03F7C11715C9B870E3B611E952A53CC998E74F02ED`.

The final LuaLaTeX log contains zero undefined control sequences, undefined citations, undefined references, multiply defined labels, rerun requests, overfull boxes, underfull boxes, or missing characters. Its remaining warnings are one inherited `inputenc`-under-LuaTeX notice, 49 inherited `fancyhdr` head-height notices, and one duplicate `page.1` PDF destination caused by the front-matter page-number transition. The page inspection found no header or content clipping from the head-height notices, and the duplicate destination does not change visible pagination or content.

Parsed all nine section files and found 16 citation commands containing 27 key tokens and 21 unique keys. Every cited key resolves to exactly one of the 412 BibTeX records. Thirteen cited keys belong to the Task 2 verified primary-source set. The eight DOI or URL fields present for the cited Task 2 records match that record exactly, with zero identifier mismatch. The final BibTeX log contains no missing-entry, repeated-entry, or warning diagnostic.

Rendered all 68 pages at 160 dpi and inspected every physical page for clipped equations, unreadable figures, split boxes, footer collisions, table overflow, and orphan headings. Dense or suspicious pages 31, 41 through 49, and 67 through 68 were also inspected individually at original render resolution. The initial visual pass found automatic word breaks in the Chapter 7, 8, and 9 display titles and a Chapter 8 running head that overlapped `Scientific Report` on physical pages 42 through 47. Display-only chapter line breaks now preserve the complete table-of-contents titles, and a shorter Chapter 8 running mark removes that collision. The corrected physical pages 31 and 41 through 49 were rerendered at 160 dpi and re-inspected; the titles, running heads, Figure 9.1, Table 8.1, equations, page numbers, and footers are legible and unclipped.

The final project wording scan reports zero banned stock phrases, UK spellings, banned LaTeX spacing macros, placeholders, prose horizontal rules, first-person claims, or unsupported promotional superlatives. `git diff --check` exited 0. No task-owned auxiliary file, page raster, contact sheet, or compiled PDF was added to the tracked diff.
