---
title: GL(K) Manuscript Verified Ledger
kind: verified-ledger
updated: 2026-06-20
tags: [project/transformer, cluster/gauge-theory, cluster/info-geometry, cluster/vfe, cluster/attention]
---

# GL(K) Manuscript Verified Ledger

Settled record for the canonical manuscripts in this folder (`GL(K)_attention.tex`, `GL(K)_supplementary.tex`, and the companion `PIFB.tex`). Everything below has been verified correct, applied as a finalized correction, or adjudicated as not-a-defect across deep-review passes 3 through 8 (plus the 2026-06-19 wiki peer review and derivation addendum).

How the peer-review skill uses this file: read it before reviewing, and treat every entry as settled — do not re-flag a verified identity as a defect, re-open an adjudicated rejection, or re-derive what is already checked, unless the manuscript text that touches the entry has changed or the user explicitly asks. After a review, append newly verified items, applied corrections, and adjudicated rejections so the next pass inherits them.

Anchors: equation labels (`eq:...`, `thm:...`) are canonical. Line numbers are as-of the seeding passes and drift as edits land — trust the label, use the line as a hint. Residuals are the numerical agreement reported by the verifying pass.

## 1. Verified equations, identities, and derivations

### Gauge invariance (Theorem 1)
- `thm:glk_invariance` trace term, Mahalanobis term, and log-det congruence `det(OΣOᵀ) = (det O)² det Σ` — sympy [addendum].
- `thm:glk_invariance` end-to-end Gaussian KL invariance including `det O < 0` — numerical to machine precision [addendum]; survives adversarial verification [wiki-pr].
- `thm:glk_invariance` chi-square f-divergence invariance — Monte Carlo [addendum].
- `eq:geometric_bias` (attn ~1120) `S(Ω) ≥ 0` with equality iff `Ω ∈ O(K)`, via `S = ½ Σ_a (λ_a⁻¹ − 1 + log λ_a)`, each term `≥ 0`, vanishing only at `λ = 1` — hand-derivation [pass5]. Transported precision `(ΩΣΩᵀ)⁻¹ ≠ Σ⁻¹` unless `Ω ∈ O(K)` — numerical [pass4].

### Attention, softmax, and the QK reduction
- `GLK-attention-softmax-KKT`: Lagrangian stationarity (incl. the `+1` constant in the normalizer), temperature softmax `β* = π exp(−E/τ)/Z`, uniqueness via PD Hessian `diag(1/β)`, substituted value `−τ log Z_i`, τ-rescaling equivalence, and entropy-drop → delta (not softmax) when `τ β log(β/π)` is removed — sympy [addendum, pass7].
- Mahalanobis identity (attn ~1078); isotropic KL expansion and cross-term transpose (~1196–1222); `eq:full_kl_general` (~1155) general untied decomposition with residual provably zero — sympy + numpy [addendum].
- Three-limit reduction: surjectivity of `M_ij` onto `GL(d)` and SVD-factorization existence — sympy + numpy [addendum]. Single-head transport `Ω = (σ² W Wᵀ)⁻¹` with square `W ∈ GL(d_k)`; multi-head head-space factor `A^a = (σ² A_K^a (A_Q^a)ᵀ)⁻¹ ∈ GL(d_head)` (thin-SVD) — numerical [residuals 3.3e-11] [pass7].
- RoPE identification (attn ~1187) — sympy + numpy [addendum]. Central-QK log-det collapse (attn ~1186): `log det Σ_j − 2 log|det U_j| = log det C` is j-independent under closure `Σ_j = U_j C U_jᵀ`, so the absorbed bias is `b_ij = −r_j/(2τ) + const` — hand-derivation [pass7].
- `eq:per_head_temperature` (G2): the full KL (carrying the `½`) divided by `κ_a √d_head` gives effective `2 √d_head`, equal to the dot-product `√d_head` softmax under the constant-key-norm condition — numerical [residual 4.4e-16] [pass6, pass3]. Two-temperature reconciliation: dot-product → `√d_k`, squared-distance keeps KL's `½` → `2 √d_k` — hand-derivation [pass3].
- ALiBi head-slope (attn ~823) correct for general head count, not only `H=8` — hand-derivation [pass6].
- Positional-bias reductions into `eq:mixture_softmax_general` (attn ~758–840) end-to-end: substituting causal `π_j=1/i`, window `π∝1[|i-j|≤w]`, ALiBi `π∝exp(-m|i-j|)`, and T5 `π∝exp(b_{i-j})` reproduces the published softmaxes exactly; the log-prior enters the logits additively as `+log π_j` with NO `1/τ` factor (dividing the bias by `τ` deviates 0.04–0.18); signs correct (ALiBi penalizes distance, T5 adds learned offset); matches Press 2022 (`-m(i-j)=-m|i-j|` on causal support) and Raffel 2020 — symbolic + numpy [residuals 5.6e-17] [pass8]. The `/τ` enters via the WLOG rescaling `eq:F_align_canonical_tau` (~768), not `eq:mixture_softmax_general` itself (~757, written τ-free) — a disclosed convention, not a defect [pass8].
- RoPE relative composition `R(θ_i)^T R(θ_j) = R(θ_{j-i})` (attn ~1875) and the `exp(φ_i-φ_j)` transpose/inverse identity on the abelian `SO(2)^{d/2}` subgroup (eq:rope_lie_algebra ~1894) — numerical [residuals 2.2e-16 / 0] [pass8].

### Free energy, envelope theorem, EM separation
- `eq:autograd_envelope_gap`: surrogate-vs-envelope gradient gap `= −τ⁻¹ Cov_β(E, ∂E/∂x)`, exact — sympy [pass7]; envelope treatment internally consistent (boxed gradient = gradient of attention-weighted surrogate; `F_red` drops `dβ` terms; coincide at joint stationarity) [addendum].
- Geometric-mean Boltzmann belief `q_i* ∝ e^{−H/2} ∏ (Ω q_j)^{β/2}` with correct `½` exponents — recomputation [pass7, pass3].
- EM separation / mean-field factorization `Q = ∏ q_i^{β}` is target-blind in the E-step; parameters move only in the M-step — recomputation [pass7].
- Gradient-descent block: all four mean-gradient terms, covariance-gradient coefficient, softmax sensitivity, α chain rule, Gaussian KL-wrt-Σ derivative, exact Fisher `σ⁻²` cancellation, and both envelope identities — sympy + NumPy central-difference [residual 1.4e-10] [addendum]. The reduced-free-energy Hessian's `−τ⁻¹ Cov_β` Schur correction — numerical [addendum].

### Covariance, Fisher, natural gradient
- Fisher mean preconditioner `G_mu⁻¹ = Σ` (App-D, attn ~1481): `G_mu = Σ⁻¹ E[(x−μ)(x−μ)ᵀ] Σ⁻¹ = Σ⁻¹` via mean score `Σ⁻¹(x−μ)` — sympy [pass6], numerical [~2e-15] [pass7]. Covariance natural gradient `δΣ = −2 Σ sym(grad) Σ` — numerical [~2e-15] [pass7].
- KL covariance gradient, the `−(1+α)` coefficient, the `α=1` and general-α convex-barycenter fixed points, and the single-KL second variation — sympy + finite differences [addendum].
- e-flat barycenter: arithmetic mean of precisions is the e-geodesic barycenter for the Gaussian exponential family (distinct from the Karcher mean); the manuscript correctly qualifies "in the natural parameterization" [addendum, wiki-pr].
- Frobenius-pullback natural-gradient metric (supp eq:pullback_metric ~618): right-trivialised Fréchet derivative `D_φ(exp)[T_a] = Ψ(ad_φ)(T_a)·exp(φ)` (Hall 2015 Thm 5.4) vs finite difference [residual ~1e-10]; trace-cyclicity collapse to `tr(Ψ(T_a)^T Ψ(T_b) exp(φ)exp(φ)^T)` exact; limits `φ=0→`Gram, skew`→I`, symmetric`→exp(2φ)` [~1e-14]; `Ψ_R=e^z Ψ_L` symbolic; `Ψ(z)=(e^z-1)/z=Σ z^k/(k+1)!` with Bernoulli inverse `z/(e^z-1)`; `G=J^T J` symmetric PSD, degenerate only at `2πi`-integer `ad_φ` resonances (rank 9→3 at θ=2π) — numerical [pass8].
- φ-gradient / KL-through-transport chain (supp App C eqs:phi_grad_complete 408–419, beta_grad_phi/reverse, mean/trace/logdet_term_phi 515–538, dtilde_mu/dtilde_Sigma) with right-trivialised `Q_a = D_φ(exp)[T_a] e^{-φ}`, including the second-order `-Σ^{-1}(dΣ)Σ^{-1}` term — finite difference, K=3 full gl(K) basis [residuals 6.8e-11 / 1.7e-10 / combined 5.0e-10] [pass8]. (Distinct from the ledger-verified μ/Σ gradients; the gauge-frame gradient through dexp was previously unverified.)
- Cartan/Killing preconditioner block (supp eqs:cartan_decomposition, P_sym ~588, killing_metric ~599): GL(K) Killing form `B=2K tr(XY)-2 tr X tr Y` vs `tr(ad_X ad_Y)` [3.6e-15]; `tilde_g_ab` PD on `sl(K)` (one zero eig on the center); `P_sym=½G^{-1}(G+S)` idempotent; Ad-invariance iff `g^T g` scalar (both directions) — numerical [pass8].

### SPD geometry, transport, dexp
- SPD retraction = Pennec affine-invariant exponential map (supp eq 643/645): `Σ^{1/2} exp(τ B) Σ^{1/2}` is the exact AIRM exp-map; clipping merely rescales the tangent — numerical [residual 6.3e-14] [pass7], hand [addendum, wiki-pr].
- `eq:dexp_so3` Rodrigues SO(3) dexp closed form and small-angle Taylor, resting on `ad_φ³ = −θ² ad_φ` — numerical [residual 1.6e-16 / machine precision] [pass7, addendum]. Higham block-matrix Fréchet representation — numerical [residual 3e-126] [addendum]. The general `(e^{ad}−1)/ad` series (supp ~451) covers `N>3`; the body restricts the closed form to SO(3) [addendum, wiki-pr].
- Connection transformation law `A^(i)_μ = A^(j)_μ + U_j⁻¹ (Ω_ij⁻¹ ∂_μ Ω_ij) U_j` (left-invariant) — finite-difference, K=3 [residual 3.4e-10] [pass7]. Cocycle, holonomy, and curvature identities — numerical [residuals 9.6e-16, 1.5e-15, 5.7e-10] [pass7].
- Holonomy connection is matrix-valued / non-abelian via `Σ_a δ_ij^a G_a` (commutator norm 0.53, not zero) [pass3]. Per-head holonomy: squared Frobenius norm is additive across blocks, so the aggregate is the root-sum-of-squares [2.084 vs naive sum 3.592] [pass6].

### RG / CLT scaling
- `g2`: `y2 = −1` under the manuscript's own `n²`-edge averaging — `g2` averages `|A||B| = n²` edges and contracts as `(n²)^{−1/2} = n⁻¹`, vs `g1` averaging `n` covariances → `n^{−1/2}` — numerical [y2 measured −1.000 over 2000 trials; intrinsic-mean variant −1.014] [pass3].
- `g3`: under the cocycle `A+B+C = 0` the order-ε term of `H−I` vanishes, leaving `H−I = ½[A,B] + O(ε³)`, so `g3` is genuinely quadratic and `y3 = −1` is consistent with `y1 = −½` — sympy [addendum].
- Meta-agent moment matching `Σ_A = mean(Σ_i) + Var_A(μ)` (eq:meta_agent_beliefs / _supp ~950): exactly the covariance of the equal-weight Gaussian mixture (Euclidean law of total covariance); symbolic residual identically zero, MC deviation pure sampling noise `~1/√N`; the population `1/|A|` normalization is the unique correct moment-matching choice (the `1/(|A|-1)` variant carries a systematic ~4% bias); `μ_A` exact first moment; `g_1^(emer)=‖Var_A(μ)‖/σ²` the anisotropic component, normalized consistently with `g_1^(orig)` — sympy + Monte Carlo [pass8]. Meta-agent transport `Ω_AB` averages `n²` edges → `n^{-1}` and the holonomy linear expansion `(I+e1)(I+e2)(I+e3)-I = e1+e2+e3+O(e²)` giving `y3=-1`/`-2` — re-confirmed numerically [slope -0.9999] [pass8].
- Off-diagonal codimension arithmetic (attn 1826): `512² − 8·64² = 229,376 = 87.5%`; sigmoid `n_s=2` reduction exact [5.6e-17] [pass8].

### App H (KL uniqueness among f-divergences)
- Functional derivative, coefficient-2 forward implication, rearrangement to a constant, residual-sum subtraction, the `f'(1)=0` WLOG footnote, the `+1` absorption, the envelope-theorem toy, and the open-subinterval analytic-continuation step (`r(c)` non-constant real-analytic, image an interval by IVT) — hand-derivation [addendum]. Generator `f(t) = t log t − t + 1`, `f'(1)=0`, divergence-preserving shift `f → f + c(t−1)` [pass6]. Reverse implication rigorous after the pass-6 realizability fix (`q_j = Ω⁻¹ q̄` equalization); proof non-circular [pass7].

### App I decode constant (genuine pass-7 defect, now fixed)
- `eq:supp_kl_decomposition` (supp ~1407): the cross-entropy identity `E_q[log p] = −D_KL(q‖π_v) − H(q)` is exact, so the decode constant is `C = 0`, not `−(K/2)log 2π − K/2` — sympy + Monte Carlo [residual with the stated C is +5.6758 for K=4; with C=0 it is machine zero] [pass7]. The defect was inert downstream (C is v-invariant and cancels in the softmax).

### Numerical reproductions (pass 7)
- Every printed PPL ratio, random-chance factor, head-count percentage, Bonferroni threshold, parameter overhead, temperature-dispersion / CV statistic, the "within 19%" claim, and the grand-mean 95% CI reproduces from the reported setup [pass7]. Residual-temperature arithmetic `(19−16)/16 = 18.8% ≈ 19%`, `2√d_k = 16` [pass7]. Entropy ratio `1.784/1.774 = 1.006` (ratio of means) vs `1.076` (per-head mean of ratios) [pass6].

## 2. Settled constructions and applied corrections

- Decode constant set to `C = 0` with a one-clause explanation; the `±C` appearances at decomposition, softmax cancellation, and accuracy-term identification remain correct with C=0 [pass7].
- Connection transformation law: left-invariant form applied, with a note giving the adjoint form recovered under the right-invariant convention [pass7].
- Table 1 positional-bias rows: ALiBi, T5, and sliding-window reclassified D → S (the framework supplies the additive-log-prior form, not the specific bias); causal mask and RoPE kept D [pass6].
- "L-layer transformer ↔ L natural-gradient steps" reframed as a structural correspondence (untied per-layer weights descend different functionals); the tied-weight inner loop is the genuine single-`F` iteration [pass6].
- "learning is symmetry breaking" softened to "learning lifts a gauge degeneracy"; conclusion "reveals" → "suggests"; explicit-not-spontaneous (Elitzur) framing surfaced [pass4, wiki-pr].
- Notation: sigmoid glyph → `\varsigma` (7 sites) to clear the `σ` collision [pass5]; `d_k` overload disambiguated (ambient `K` vs per-head `d_head = K/H`) [pass5]; softmax Lagrange multiplier `λ → ν`, key-norm coefficient `λ → λ_K`, SPD clip radius `ρ → ρ_tr` [pass5].
- Bibliography: six works cited under dual keys canonicalized onto the ecosystem key (no entries deleted); Sengupta-Friston 2017 preprint to be added and the mis-pointed `SenguptaFriston2018` key fixed [pass5, wiki-pr].
- `eq:vfe_glu` gate-energy subscripts changed to transported precisions `(Ω Σ Ωᵀ)⁻¹` [pass4]. Ablation advantage corrected to 1.94×/1.99×/2.04× against GL(15) test PPL 71.6 [pass4].
- Per-head holonomy text corrected from "sum of per-head deviations" to root-sum-of-squares; App-D Fisher one-line derivation added inline; entropy-ratio overline restored with the ratio-of-means noted [pass6].
- Residual temperature relabeled as the empirical optimum `τ = 19.0` with the theory value `2√d_k = 16` stated alongside [pass6, pass7].
- `rope_full_gauge = False` (bool), not the string `'off'`, matching `config.py:151` [pass7]. SPD trust region documented as a diagonal L∞ clip (per-coordinate `δσ/σ` clamped to `[−ρ, ρ]`; diagonal default 5.0, full 2.0) [pass7].
- App G "Vacuum State" reworded to "common gauge orbit" / "distinct directions of a common norm sphere"; the ℓ=4 SO(3) irrep has no nonzero invariant vector (stacked-generator kernel and Haar projector both confirm) [pass7].
- Multi-head block transport `W → A` (thin-SVD factor genuinely in `GL(d_head)`) [pass7].
- Pass-3 confirmed defects fixed: phantom `sanitize_sigma` → real inline `clamp(min=eps, max=σ_max)` path; phantom `retract_to_principal_ball` → `retract_phi` (Frobenius trust-region rescale, no modulo/antipodal); Regime II is a single bilinear `connection_W` `nn.Parameter`, not a `connection.py` "MLP mode"; stale glossary `κ ∝ σ²` clause deleted; broken cross-refs fixed (`sec:rope_gauge → sec:rope`, missing `fig:glk_pca_frames`, missing `sec:keynorm_bias_supp` label); undefined `d_q`/`d_p` → `K_q`/`K_p` [pass3].
- Value-aggregation gloss (attn 1043): `mixture variational model` → `generative mixture`, so the one-liner agrees with its own `sec:value_aggregation` (1327). `μ̂_i = Σ_j β_ij Ω_ij μ_j` is the responsibility-weighted mean of the generative mixture components, not `E_Q[k]=μ_i` (the mean-field posterior `Q=q_i·β` has `k`-marginal `q_i`) [pass8].
- Pass-8 expository/citation polish (applied to the canonical vault manuscripts on user request): Knapp bibref repointed `[Prop.~1.93]` → `[Ch.~VI]` for the Cartan-involution-modified form (Prop 1.93 is a Ch I topological-groups result; the Cartan-involution / positive-definite-`B_θ` material is Ch VI — exact eq/cor, ~(6.13)/Cor 6.18, left for author refinement) (supp 596); temp-dispersion "ranking anti-correlates…systematically" softened to "negatively associated (Pearson `r=-0.87`, Spearman `ρ=-0.6`)" (supp 878); key-norm `0.256` relabeled the between-head mean of *absolute* correlations (was mislabeled "single-head summary"), distinguished from the signed population magnitude `0.475` with a Jensen note (supp 924); Cartan projector `P_sym` noted to retain the center/dilation direction (`P_sym(I)=I`, full `Sym(K)⊕ℝ·I`) (supp 588); ALiBi/T5/window reductions cross-referenced to the temperature rescaling `eq:F_align_canonical_tau` for the `/τ` (attn 778); pullback-metric degeneracy at `2πi`-integer `ad_φ` resonances + `G+εI` regularization noted (supp 621). All off-path or expository; no equation or exponent changed [pass8].
- Overall: the core mathematics is sound — no sign, transpose, or index error was found anywhere across the eight passes' proofs; the manuscript is mathematically exhausted at the level of its analytic derivations (pass 8 completeness critic: no remaining un-recomputed load-bearing identity). Residual weaknesses are expository, documentary, or bibliographic [addendum, wiki-pr, pass7, pass8].

### Round 9 (2026-06-20, `/peer-review` deep pass; PIFB.tex; applied to the vault) [pass9]
Round 9 weighted the RG-appendix proof machinery and the philosophy sections (the least-saturated surfaces). The RG theorems `rg_pushforward → rg_detector_retention` and the mean-field lemmas (`lem:mf_fixedpoint`, `lem:shadow_mf_optimum`) were swept at proof depth for the first time and found sound; no new defect in any core result. Applied corrections (all PIFB.tex line refs):
- Norton 1993 (~3455): the deflationary "general covariance = notational requirement" horn is Kretschmann's; Norton's survey resists it and defends a substantive (background-independence) sense. Reworded; the (a)/(b) distinction the cognitive-shareability argument needs is retained.
- Underdetermination equation (~3624-3632): the displayed `KL(p_emp‖p_s_i)` equality is equal empirical adequacy (= equal expected log-likelihood), not equality of observation marginals / permanent Quinean equivalence (equal KL does not entail equal marginals — counterexample verified). Relabeled as adequacy; strong equivalence now defined as identical marginals; "tie that exact empirical equivalence leaves open" → "equal empirical adequacy."
- Kinetic prose (~2142): "inverse prior precision" → "prior precision" (coefficient `M_μμ = Λ̄_p = Σ̄_p⁻¹`, the precision; contradicted its own parenthetical).
- IIT composition (~3544): IIT's composition axiom is intra-complex (overlapping distinctions/relations within one experience), not cross-scale agent nesting; reworded to "parallels its composition only at the part-whole level … without realizing IIT's intra-complex structure."
- Pullback horizontal block (~2839): clarified the identical block holds under the shared principal bundle `E_model = P ×_G B_model` adopted in this section, distinct from the independent-frame `φ̃` option (line 626) used by the implementation's model dynamics. (Panel rated this high/medium as a geometric error; reviewer demoted to a clarity clause — 2839 is correct as written under the section's shared-P convention.)
- Minor: sectoring cross-ref → "spectrum of Eq. induced_metric_full" (3133, only that site); Pinsker first-power composition `√2/12` not cubed `√2/24` (5054); env coupling `β_{i,e_k}=1` is a fixed coupling outside the row-simplex `Σ_j β_{ij}=1` (1474); `τ=κ√d_k` is the canonical `κ√K_q` specialized via `K_q→d_head` (1882); Killing-form remark qualified to `K_q≥3` (constant `K_q−2`; `so(2)` abelian → Killing ≡ 0), trace form `−tr(φ̇²)` PD for all `K_q≥2` (2140).
- Clean on Round 9 (swept, no surviving findings): RG gauge covariance / coarse-graining transport; covariance dynamics + quadratic-forms-to-transported-KL; free-energy functional core (mixture energy, complete F, envelope theorem, state-dependent prior precision, symmetry breaking); attention core (zero-d limit, untied-QK, dot-product recovery, value aggregation); signature worked-example identities; RG-appendix formal proofs and mean-field lemmas.

## 3. Adjudicated rejections (raised, decided not-a-defect — do not re-raise)

- The "missing" entry point `transformer/vfe/train_vfe.py` — it is the separate `epistemic-geometry` release repo named at line 2423, not this clean-room V3; a redirect to `train_vfe3.py` would be actively false [pass4].
- The "obvious" textbook adjoint connection law `Ω A Ω⁻¹ + (dΩ)Ω⁻¹` — belongs to the right-invariant convention and fails here (residual 29.1); switching the whole section to the right-invariant look is author preference, not a defect [pass7].
- Two-channel "complete free energy" display omitting entropy terms — candid already (entropy lives in `eq:J_i_supp` and the companion; `γ_ij = 0` throughout) [pass7].
- G1 untied-QK "joint realizability / expressive power identical to learned `W_Q W_Kᵀ`" strong framing — the thesis is proven per-pair and the family is rank-structured by construction; only a minor closing-analogy imprecision remains [pass6, pass7].
- GT-1 per-head temperature "off by a factor of two" — the two conventions are the same distribution under the key-norm condition [4.4e-16] [pass6].
- VI-1 query-side update "breaks Neal-Hinton monotonicity" — the manuscript never invokes monotone descent and already caveats the partial update [pass6].
- GT-2 / GT-3 (per-head-holonomy "vacuous"; "irrep block mislabels the Levi subgroup") — presentation nits; the `H` blocks are pairwise-inequivalent irreps under the acting `GL(d_head)^H`, so the decomposition is genuine [pass6].
- `g2` CLT "should give `y2 = −½`" — rejected: the `n²`-edge averaging gives `n⁻¹`; the only residual is an expository main-text gap (port the edge-count argument, change no exponent) [pass3, pass7]; the RG `y3` deviation thread is adequate as written and re-confirmed numerically [pass7].
- Cencov = Chentsov "two independent sources" — single duplicate-key pair already counted [pass5]. App-H non-normalizable-neighbor objection — the construction is admissible [pass5]. Strict-convexity "missing N≥2/support" — already stated at line 746 [pass5]. The O(K) reflection-extension holonomy-sign nit and the sliding-window boundary-normalizability nit — not applied [pass5]. "Surpassing classical baselines" superlative — supported [pass5]. SO(N)/SO(3) Rodrigues heading — the body already restricts the closed form to SO(3) [pass5, addendum, wiki-pr].
- "Holonomy collapses to an abelian phase" — misread `δ·G` as a scalar; it is the `Σ_a δ^a G_a` basis contraction, non-abelian (commutator 0.53) [pass3]. "Coarse transport leaves the structure group, invalidating the RG exponents" — terminology nit; exponents unaffected (intrinsic mean y2 = −1.014) [pass3, wiki-pr]. "Holonomy-compositionality is unfalsifiable" — `sec:flat_bundle` already demotes it to an open question with a concrete Regime-II measurement [pass3]. "Attention IS variational inference contradicts the D/S/I taxonomy" — the copula attaches to the D-tier attention rule, not the S/I architecture components [pass3]. "First-principles omits the approximations used" — every instance modifies the theory object, and each approximation is disclosed [pass3]. von Oswald ICL-as-gradient-descent "absent" — already logged in prior pass 1, and the finding cited the wrong bibkey [pass3].
- `max_condition = 10.0` "in the V3 code" — belongs to the VFE_2.0 checkout; V3 has only an `eps = 1e-6` eigenvalue floor and `σ_max = 5.0`, no condition cap [wiki-pr]. "`kappa = 10` clamps anisotropy and undermines GL(K)" — a condition cap bounds eigenvalue ratios within a single Σ, not the cross-token gauge structure [wiki-pr]. Bonnabel "uncited" and Clark "uncited" — grep errors; cited at lines 60 and 58 [wiki-pr]. Aguilera dense-coupling as a "contradiction" — the manuscript engineers a consensus functional rather than deriving belief-encoding from NESS, so it is suggestive by analogy [wiki-pr]. Hard "objective content = gauge-invariant content" realist over-claim — the body is more guarded and does not actually state the hard identification [wiki-pr].
- Meta-agent "second moment" wording (supp 954) — loosely the *central* second moment (covariance); the parenthetical "law of total covariance" disambiguates, so left unchanged [pass8]. (The temp-dispersion, key-norm, Cartan-projector-scope, and pullback-resonance items raised in pass 8 were not defects either, but were applied as expository clarifications on the user's request — see §2.)
- `(det Ω)²` vs `|det Ω|` "inconsistency" (attn 554/566) — two genuinely distinct quantities the manuscript never equates [addendum]. "GL(K) is the symmetry group" — the text consciously restricts to linear actions [addendum]. f-divergence corollary change-of-variables "omission" — a standard textbook fact, expository compression [addendum]. softmax-KKT nits (per-row separability, KKT barrier argument, "WLOG" on τ) — already established by the single-query construction and the spelled-out τ-dependence [addendum]. RoPE "known gap" — orthogonal frames rotate Q and K symmetrically [addendum]. Residual-as-Euler-step, barycenter-ordering, and diagonal-projection flags — labeled S, already led with the `α=1` fixed point, and assert no false equivalence [addendum].

### Round 9 rejections (raised, refuted on adversarial verification — do not re-raise) [pass9]
- shadow_mf `q`-glyph "collision" (PIFB ~4786-4791) — misread: `q_i^{(s)}` is the appendix's local mean-field variational factor (posterior glyph for a posterior); the lemma's content is exactly `q_i^{(s)} → Ω[q_parent] = p_i^{(s)}`, with the strict MF fixed point given the distinct symbol `q_i^{(s),MF}`. No collision.
- RG `epsilon_I` normalization "inconsistency" (PIFB `thm:rg_detector_retention`) — severity-inflated: `epsilon_I` is an explicitly schematic, proposition-grade small-parameter (the definition gives an equivalent un-normalized form under the same symbol; constants `C_1..C_6` are smoothness-class-dependent and not derived). No threshold or result depends on the normalization.
- Gauge-curvature Wilson proxy `1−W/K_q` "sign-broken on noncompact GL⁺" (PIFB ~3415) — settled rounds 7-8: the proxy is scoped to the compact reduction (3403); the noncompact/boost sector uses the bounded squared-Frobenius form (885). Arithmetic correct but re-litigates closed scoping.
- Signature postulate count "2/3/7 inconsistent" (PIFB 2923/3584/3667) — misread: differently-scoped nested subsets ("two extra" = marginal cost of the complexified route over the real route; 3667 enumerates all seven on top of the group-theoretic prerequisite). Recomputed signature identities check out.
- Consciousness prediction "does not discriminate conscious from non-conscious" (PIFB ~3542) — misread: a necessary-condition, within-system state contrast (waking vs anesthesia in the same substrate; Mashour2020/Luppi2021), not a sufficiency/discriminator claim; the rock has no conscious/anesthetized pair. The richer discriminator (content richness vs effective dimensionality) is already at 3546.
- QBism "resists consensus" misattribution (PIFB ~3376) — misread: the manuscript's own gloss ("no agent-external shared state to which separate agents are converging") is QBism's actual position; Schack 2024 supports the contrast ("no hidden mechanism ensuring convergence"). Already framed as "a contrast with QBism rather than an extension."

## 4. Genuinely open (NOT settled — legitimately reviewable / pending)

- RoBERTa per-head temperature triple (median 25, std 15.3, CV 0.787) vs `τ_opt = 29.0` — not self-consistent; blocked on the underlying distribution, cannot be reconciled without fabrication [pass4].
- K-sweep perplexities (64.9 at K=120, 194.5 at K=10) disagree with the cited CSV — in-source TODO, blocked on the finalized CSV [pass4].
- SPD-retraction constants in the supplement (`eps_SPD = 1e-4`, `kappa_max = 1e4`) do not match the released code (1e-6 floor, no condition cap) — documentation gap, author TODO already flags it [pass3, addendum].
- Overlooked-connection citations drafted but not inserted (need bib entries + author vetting): Dong / Cordonnier-Loukas 2021, Geshkovski 2023, Mehta-Schwab 2014 / Bény-Osborne 2015, Voita 2019, Sengupta-Friston 2017, and the Karcher / Moakher / Jeuris barycenter contrast [addendum, wiki-pr, pass5].
- `g2` main-text expository gap: the `n²`-edge justification lives only in the supplement; porting it into the main text before `eq:rg_scaling` would close the gap without changing any exponent [pass3].
- Knapp citation exact eq/cor (supp 596): the bibref is now repointed to `[Ch.~VI]` (applied, §2); refining it to the precise `~eq.~(6.13)`/`Cor.~6.18` for the Cartan-involution definition is left for the author to confirm against their copy. The algebra of eq:killing_metric is verified correct [pass8].
- RG graph-based `y3=+0.17` fit window (supp tab:rg_flow_supp / 1041): reproduces only from a levels 1–5 log-log fit, while the caption states the `y3` fit uses levels 0–5 (which gives ~+0.6–0.8); `y2=-0.66` is consistent with the full 0–6 window. Now recorded in the in-source `DATA-PENDING` TODO alongside the `g1_tot` column violation [pass8]; blocked on the finalized CSV (state the per-exponent window when re-extracting).

## Provenance

Seeded 2026-06-20 from the V3_Transformer review record: `docs/reviews/2026-06-19-glk-attention-wiki-peer-review.md`, `2026-06-19-glk-attention-deep-derivation-addendum.md`, and `2026-06-19/20-glk-attention-deep-review-pass{3,4,5,6,7}.md`, plus `docs/edits/2026-06-20-edits.md`. Extraction was per-report and synthesized with cross-pass deduplication. Updated 2026-06-20 with pass 8 (`docs/reviews/2026-06-20-glk-attention-deep-review-pass8.md`): the three pass-7 completeness-critic residuals verified end-to-end, five further blocks recomputed fresh, one applied fix (value-aggregation gloss), and the Knapp-citation / RG-fit-window items opened.
