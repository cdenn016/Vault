\chapter{Numerical Provenance}
\label{app:numerical-provenance}

The reproducibility package is part of the manuscript record:
\begin{center}
\footnotesize
\begin{tabularx}{0.94\textwidth}{@{}>{\raggedright\arraybackslash}p{0.40\textwidth}X@{}}
\toprule
\path{verification/claims.json} &
claim inventory, seeds, sample counts, tolerances, mappings, and open
obligations \\
\path{verification/run_checks.py} &
single deterministic CPU runner \\
\path{verification/current-results.json} &
current environment, source and protocol hashes, individual outcomes,
residuals, and overall result \\
\path{verification/VERIFICATION.md} &
human-readable protocol and interpretation \\
\bottomrule
\end{tabularx}
\normalsize
\end{center}

From the repository root, the declared run is
\begin{verbatim}
& "C:\Python314\python.exe" manuscripts/gauge_vfe_rg/verification/run_checks.py
\end{verbatim}
The generated JSON record is the authority for exact counts and numerical
values; this appendix intentionally does not duplicate seed lists or result
tables that can drift.

\paragraph{Frozen factorization-gap stress protocol.}
The check \texttt{CHK-CG-FACTOR-GAP-STRESS-3138} is a new deterministic
protocol, not a reconstruction of an undocumented historical run.  Seed
\texttt{20260803} fixes exactly $3{,}138$ case identities: $2{,}400$ general,
$200$ exactly block diagonal, $200$ nearly decoupled, $120$ scale, $120$
permutation, $80$ nested-refinement, and $18$ 100-decimal-digit control cases.
Every stratum covers dimensions 2 through 16 and condition-number tiers
$1,10^2,\ldots,10^{14}$.  Each record binds the regenerated binary64 matrix and
partition by SHA-256, and repeated executions must agree exactly.

The ordinary value path uses Cholesky triangular solves, canonical-correlation
singular values, and \texttt{log1p(-rho**2)}.  Near the floating-point boundary,
including a raw excursion above one, the runner compares two binary64 solve
backends, applies a $64n\epsilon_{\rm mach}$ operational rejection guard, and
uses a 200-decimal-digit evaluation of the exact stored binary64 input for the
reported gap.  The value path also falls back when
$\kappa_2(J)\epsilon_{\rm mach}\geq10^{-4}$; this is a frozen-protocol
conditioning trigger, not a proved perturbation radius, and an in-domain case
is reevaluated rather than rejected merely because the trigger fires.  The
public diagnostic name
\texttt{residual\_derived\_clip\_bound} is retained for protocol compatibility,
but on this boundary path its value is the outward-rounded discrepancy between
the maximum raw binary64 $\rho^2$ and the high-precision exact-input $\rho^2$.
It is not presented as a general residual perturbation bound.  The frozen
admitted witness has raw excursion $2^{-51}$ and reported allowance no greater
than $9.432369402326953\times10^{-16}$; its value comes from the exact-input
fallback rather than from clipping $\rho^2$ below one.  The compatibility
fields \texttt{clipping\_applied} and \texttt{clipping\_amount} record that a
positive cross-backend excursion was admitted and its maximum excess above
one; they do not say that this amount was substituted into the returned value.
Exact-input domain violations and raw excursions whose discrepancies exceed
the guard are rejected.  Cholesky and solve residuals remain local backward
diagnostics; the protocol does not promote them to canonical-correlation or
gap forward-error bounds.  Their finite-health gate is $64\gamma_n$, with
$\gamma_n=n u/(1-n u)$ and binary64 unit roundoff
$u=\epsilon_{\rm mach}/2$.

\paragraph{What a passing run means.}
It means that the current deterministic, double-precision CPU
implementations satisfy their declared finite checks and controls within
their recorded tolerances.  Those checks corroborate algebraic identities,
exercise finite witnesses, and detect regressions in the encoded endpoints.
They do not prove a theorem, genericity, an asymptotic limit, universality,
or a physical interpretation.  Analytic claims rest on derivations or
eligible cited results, not on numerical agreement.

\paragraph{Freshness and scope.}
The result artifact records interpreter and dependency versions and binds
the discovered manuscript sources and verification protocol by SHA-256.
Any source, protocol, input, dependency, or environment change requires a
new run before the numerical evidence is current.  The suite uses no GPU and
makes no GPU claim.  A missing protocol for an empirical statement remains
inconclusive even when every implemented check passes.
