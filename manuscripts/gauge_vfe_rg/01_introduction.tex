\chapter{Introduction and Reading Guide}
\label{ch:introduction}

This document develops one construction and follows it as far as it goes. A population of agents is
carried by associated law objects over a contextual base, with smooth statistical bundles selected
only under explicit differentiability hypotheses; a single fixed normalized law
generates their latent states and observations; a single correlated recognition law approximates the
resulting posterior; and one exact identity relates the two. Everything after that is either a
consequence of that identity, a declared restriction of the family it is evaluated on, or a
statement about what happens to the whole structure when the population is described at a coarser
resolution.

The last of these is the reason the document exists. Coarse-graining a population of inferring
agents is a renormalization, and asking which coarse descriptions the exact bound licenses turns out
to be a sharper question than it first appears. Some operations that are routinely called
coarse-graining move the evidence, some move only the bound, and some are not admissible at all.
Separating them is most of the work, and the separation has consequences the informal picture does
not anticipate.

\section*{The epistemic contract}

The single most important convention in this document is that every non-trivial claim carries a
visible status, and that the statuses are not interchangeable. They appear as a bracketed tag
immediately after the statement they govern, and they are also named in the surrounding prose so
that the discipline survives being read aloud.

\begin{sciencetable}{Status tags used throughout. A claim without a tag is an error, and a
conjecture written in the grammar of a theorem is a worse error.}
\label{tab:status-taxonomy}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{0.19\textwidth}>{\raggedright\arraybackslash}X}
\toprule
\textbf{Tag} & \textbf{What it promises the reader} \\
\midrule
\status{ESTABLISHED} & Proved here, or a standard result cited to a source that has been checked. \\
\status{DEFINITION} & A declared type, construction, or convention. Nothing is being proved and the text says so. \\
\status{HYPOTHESIS} & A restriction the development adopts by choice. What it excludes, and where it is used, are stated. \\
\status{CONJECTURE} & Believed and precisely stated, but not proved. Stated sharply enough to be attacked. \\
\status{NUMERICAL} & Supported by computation only, with its measurement, seed, and control reported. Computation is not proof. \\
\status{OPEN} & Unsettled. What would settle it, and what obstructs it, are named. \\
\status{NOT-CLAIMED} & A statement the development deliberately declines to make. Declining is not refuting, and the text says which it is doing. \\
\bottomrule
\end{tabularx}
\end{sciencetable}

Two habits follow from the table and are worth stating explicitly, because they are the ones most
easily lost.

A hand-wave is treated as a debt rather than a style. Any step made by gesture rather than argument
is either discharged, by supplying the proof or computing the constant or naming the exact condition,
or else converted into a declared gap that states what holds, under what hypotheses, what remains
unproved, and what would close it. Weakening the verb is not a permitted third option: a sentence
reporting that something presumably converges is worse than one reporting that convergence is open,
because the first conceals the debt inside a construction that reads like a result.

Declining a claim and refuting one are kept apart. Both occur here. Chapter~\ref{ch:obstructions}
records constructions that fail, and several chapters record adjacent statements the development
deliberately does not make. Conflating the two would misrepresent the theory in opposite directions,
and each chapter closes with a register listing its own results and their statuses so that the
distinction can be audited rather than trusted.

\section*{What is established, and what is not}

It is worth saying at the outset, in compressed form, where this development actually stands. Each
claim of this section carries its own tag, and the register at the end of the chapter collects them.
The foundations are secure: the typed construction, the fixed normalized generative law with its
gauge covariance, and the exact evidence identity are proved under hypotheses that are stated and
used. \status{ESTABLISHED} The Gaussian realization is likewise proved, with one qualification that
matters and is easy to miss, namely that the interaction family on which the entire second half
operates is a \emph{declared subfamily} adopted by choice and not a consequence of the
linear-Gaussian model. \status{HYPOTHESIS} Chapter~\ref{ch:gaussian} exhibits the parameter choices
under which the assumed form fails.

The coarse-graining results are proved, and the one most likely to be misread is negative. Closure of
the interaction family under aggregation does hold, exactly. \status{ESTABLISHED} It does not,
however, characterize the family: a one-parameter continuum of alternatives is equally closed, and
what actually selects the form is a symmetry rather than a renormalization property, which is itself
a proved negative rather than a gap. \status{ESTABLISHED} The coarse operator itself is not new, and
this document declines to claim it, citing it to the numerical-analysis literature
instead. \status{NOT-CLAIMED}

The renormalization results are the least settled, and the document is arranged so that this is
impossible to overlook. The rescaling that turns aggregation into a flow is declared, not
derived. \status{DEFINITION} The bi-additive parameter family is proved closed under aggregation; a
fixed ray is typed only on an identified hierarchical endomorphism, and the homogeneous
complete-graph case supplies one such operator-level instance. \status{ESTABLISHED} That ray lies on
the boundary of the Gaussian probability domain and is not a normalized recognition law.
\status{ESTABLISHED} Attraction is unsettled. \status{OPEN} Two further gaps sit beside it and are named here because a reader
calibrates on this paragraph: whether a converging orbit reaches a limit that is unique within its
component, and whether that limit depends on the blocking scheme rather than on the population, are
equally unsettled, and the statement that a connected population displays no observable variation in
its limiting operator class is the joint consequent of all three rather than a proved
result. \status{OPEN} Finite blocking experiments move two diagnostics toward the bi-additive form
against matched controls, but a finite population has no infinite blocking orbit and those
measurements do not establish convergence, uniqueness, or scheme independence.
\status{NUMERICAL} Identifying a repaired fixed object with a physical law or empirical regularity is
an interpretive and empirical proposal for which this document supplies no validation criterion.
\status{OPEN}

\section*{Scope}

The ambient typing here permits general declared law and Markov-kernel fibers, while the smooth,
exponential-family, and Gaussian tiers require progressively stronger hypotheses and the explicit
renormalization results are chiefly Gaussian operator statements. The document is not a commentary on, a defense of, or a crosswalk to any
particular earlier formulation, and it borrows no term structure, no engineered objective, and no
implementation detail from one; the document declines that reading rather than arguing against
it. \status{NOT-CLAIMED} Where a prior construction is refuted, in
Chapter~\ref{ch:obstructions}, the refutation is stated as a general structural result about the
class of constructions it belongs to, because that is the form in which it is actually true and the
form in which it is useful to a reader who has never seen the specific case.

Two chapters are load-bearing for readers in a hurry. Chapter~\ref{ch:elbo} contains the identity
everything rests on. Chapter~\ref{ch:coarsegraining} contains the operations, their costs, and the
result that closure is not a characterization. A reader interested only in the renormalization may
begin at Chapter~\ref{ch:gaussian} and refer back, at the cost of taking the typing on trust.

\section*{Status register}

This chapter proves nothing of its own. Its register therefore records the summary claims made above,
each with the chapter that carries the argument and, for anything not \textsc{established}, what is
owed there. A row here is a pointer, never a substitute for the register of the chapter it names.

{\small
\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.36\linewidth}>{\raggedright\arraybackslash}p{0.16\linewidth}>{\raggedright\arraybackslash}p{0.42\linewidth}@{}}
\caption{Status register for Chapter~\ref{ch:introduction}. Every claim of the overview section
appears exactly once, with the chapter that discharges it.}
\label{tab:intro-register}\\
\toprule
\textbf{Summary claim} & \textbf{Status} & \textbf{Where it is settled, and what is owed} \\
\midrule
\endfirsthead
\multicolumn{3}{@{}l}{\emph{Status register for Chapter~\ref{ch:introduction}, continued.}}\\
\toprule
\textbf{Summary claim} & \textbf{Status} & \textbf{Where it is settled, and what is owed} \\
\midrule
\endhead
\bottomrule
\endlastfoot
The status taxonomy of Table~\ref{tab:status-taxonomy} & DEFINITION & A declared convention. Nothing is proved by it, and it is the only convention this chapter introduces \\
Typed construction, fixed normalized generative law with its gauge covariance, and the exact evidence identity & ESTABLISHED & Chapters~\ref{ch:geometry} to~\ref{ch:elbo}, under hypotheses stated and used there. Nothing owed here \\
The interaction family is a declared subfamily, not a consequence of the linear-Gaussian model & HYPOTHESIS & Chapter~\ref{ch:gaussian}, which exhibits the parameter choices under which the assumed form fails and names the hypothesis at every later use \\
Closure of the interaction family under aggregation & ESTABLISHED & Chapter~\ref{ch:coarsegraining}. Nothing owed here \\
Closure does not characterize the family; a symmetry selects the form & ESTABLISHED & Chapter~\ref{ch:coarsegraining}, with the closed continuum as the explicit witness. A proved negative, not a gap \\
Priority for the coarse operator & NOT-CLAIMED & Chapter~\ref{ch:coarsegraining} cites it to the numerical-analysis literature. Declining a claim is not refuting one \\
The rescaling that turns aggregation into a flow & DEFINITION & Chapter~\ref{ch:renormalization} declares it. It is the externally declared scale Chapter~\ref{ch:coarsegraining} records as required and does not supply \\
Bi-additive closure and the identified homogeneous fixed ray & ESTABLISHED & Chapter~\ref{ch:renormalization}. Closure is not a fixed point on changing spaces; the fixed-ray statement uses an explicit hierarchical identification and remains operator-level \\
Attraction to that fixed point & OPEN & Chapter~\ref{ch:renormalization}, Open~11.5. A basin is owed: an endomorphic family, then a contraction argument on it \\
Uniqueness of the limit within a component, and independence of the blocking scheme & OPEN & Chapter~\ref{ch:renormalization}, Open~11.13 and Open~11.14. A uniqueness theorem on a declared class, and an invariance theorem over admissible blockings, or a counterexample to either \\
No observable variation in a limiting operator class across a connected population & NOT-CLAIMED & Chapter~\ref{ch:renormalization} proves it only as the implication of Cor.~11.15, whose three antecedents are all open. Reading the operator as a law adds the separate probability-repair obligation \\
Finite diagnostics move toward the bi-additive form & \status{NUMERICAL} & Chapter~\ref{ch:renormalization}, at seed $20260728$ against size-matched null systems. A finite blocking sequence does not establish asymptotic convergence \\
A repaired fixed object is a physical law or empirical regularity & OPEN & Chapters~\ref{ch:renormalization} and~\ref{ch:philosophy}. R15 remains empirically and interpretively unvalidated; a declared correspondence and discriminating evidence are owed \\
That this document comments on, defends, or crosswalks to any particular earlier formulation & NOT-CLAIMED & Declined rather than argued against. Chapter~\ref{ch:obstructions} refutes a class of constructions, which is a different act and carries its own status there \\
\end{longtable}
}
